---
quick_id: 260915-1ab
slug: rearrange-deployment-md-into-a-clear-ser
date: 2026-09-15
status: ready
mode: quick
files_modified:
  - DEPLOYMENT.md
  - README.md
  - SETUP-USAGE-DEPLOY.md
  - .env.example
requirements: [QT-01, QT-02, QT-03, QT-04, QT-05, QT-06, QT-07, QT-08]
must_haves:
  truths:
    - "A newcomer reads DEPLOYMENT.md top-to-bottom once: pick path → prerequisites → .env → their path (§4-§7) → post-install → verify → monitor (QT-01)"
    - "DEPLOYMENT.md has exactly ONE env-variable reference (§3) and ONE troubleshooting table (§11, with Path column) (QT-02, QT-03)"
    - "Each of the 4 deployment paths is self-contained, with queue-worker + scheduler setup inside its own section (QT-04)"
    - "The backup & restore runbook is its own appendix-style section (§12) with internally consistent numbering (QT-05)"
    - "Every § reference inside DEPLOYMENT.md and every DEPLOYMENT.md § citation in README.md / SETUP-USAGE-DEPLOY.md / .env.example resolves to the correct new section (QT-06)"
    - "Zero content loss: every warning, gotcha, verbatim config block, and pitfall row survives (QT-07)"
---

# Quick Task 260915-1ab — Rearrange DEPLOYMENT.md into a serial setup guide

## Goal

Restructure `DEPLOYMENT.md` (755 lines, 13 sections) into one linear, newcomer-friendly
flow — **choose path → env config → deploy (per path) → post-install → verify → monitor** —
with a single consolidated env-variable reference, a single consolidated troubleshooting
table, self-contained path sections, and all § cross-references (internal AND external)
renumbered consistently. Docs-only; zero content loss (QT-01…QT-08).

Requirement IDs used in this plan:
QT-01 serial flow · QT-02 one env reference · QT-03 one troubleshooting table ·
QT-04 self-contained paths w/ worker+scheduler inside · QT-05 backup runbook appendix ·
QT-06 cross-refs renumbered (internal + external) · QT-07 zero content loss · QT-08 docs-only.

## Context

@DEPLOYMENT.md — the target; read fully before Task 1
@SETUP-USAGE-DEPLOY.md — its L167/L214/L221 cite DEPLOYMENT.md §12/§13/§4.2/§4.3; DEPLOYMENT.md §7 points back at SETUP §5.3
@.env.example — L42/L94/L96 comments cite DEPLOYMENT.md §11.9 (×2) and §11.7
@README.md — L388 cites DEPLOYMENT.md §11 (L28/L94/L396 have no § refs)

Current problems: Forge path (§3) needs §5 env + §6 HTTPS that come later; env vars split
across §5 and §12.2; troubleshooting split across §10 / §11.9 / §12.5; every § ref breaks
under renumbering. Repo has no test suite — verification is grep-based + git diff review
(per AGENTS.md "Verification" section; no pint/migrate needed for a docs-only change).

<tasks>

<task type="auto">
  <name>Task 1: Restructure DEPLOYMENT.md into the serial, path-first guide</name>
  <files>DEPLOYMENT.md</files>
  <action>
Record baselines BEFORE editing (needed by Task 3): line count (755), `grep -c '^```'`
(fence count), `grep -c '^|'` (table-row count).

Rebuild the document into 12 sections in this exact order. MOVE content; do not rewrite
prose. Change only: headings, § numbers per the mapping table, the merge glue described
below, and small path-label glue ("appendix" → "Path B" wording in the intro). Do NOT
delete any warning, gotcha, verbatim block, or table row (QT-07).

New TOC (old → new):
  1  Pick your deployment path            ← old §1 choice table + NEW 2-3 sentence reading-order
                                             note: follow §2-§3 once → do ONLY your path's section
                                             (§4-§7) → then §8-§10. Update the table's path refs.
  2  Prerequisites (production VPS)       ← old §2 verbatim (number unchanged); keep the
                                             "Docker panels need none of this" note.
  3  Prepare the production .env — one checklist for every path   ← old §5 + old §12.2 (QT-02)
  4  Path A — Laravel Forge (primary)     ← old §3 (4.1 Provision / 4.2 Queue worker /
                                             4.3 Scheduler / 4.4 HTTPS = old §3.4 + old §6's
                                             Let's Encrypt + CDN `ASSET_URL` line) + one line
                                             "storage permissions handled automatically"
                                             (moved from old §7 so the info survives).
  5  Path B — Manual VPS                  ← old §4 (5.1 Clone+install / 5.2 Nginx verbatim /
                                             5.3 Supervisor worker verbatim / 5.4 cron verbatim +
                                             the worker-vs-schedule:work callout) + 5.5 HTTPS
                                             manual (old §6 certbot line) + 5.6 Storage
                                             permissions (old §7 verbatim chown/chmod block).
  6  Path C — Docker panels: Dokploy & Coolify   ← old §12 (6.1 commit the three files —
                                             Dockerfile/compose/.dockerignore VERBATIM;
                                             6.2 Environment variables = old §12.2 intro
                                             sentence + pointer into §3.3; 6.3 Deploy on
                                             Dokploy; 6.4 Deploy on Coolify; 6.5 Worker +
                                             scheduler in containers = the schedule:work-as-
                                             long-lived-service note + the Dokploy
                                             Schedules-UI "optional extras" paragraph). (QT-04)
  7  Path D — cPanel shared hosting       ← old §13 verbatim, incl. the SETUP-USAGE-DEPLOY.md
                                             §5.3 pointer (must stay valid).
  8  Post-install essentials (every path) ← 8.1 Create the first super-admin (moved from old
                                             §8 step 2: /setup wizard + mess:create-super-admin
                                             CLI + login note); 8.2 Configure SMTP (short
                                             pointer: MAIL_MAILER=smtp per §3.1; failure-email
                                             detail stays in §12.7).
  9  First-deploy verification            ← old §8, minus the super-admin how-to now in §8.1:
                                             step 2 becomes "log in as the super-admin created
                                             in §8.1"; renumber remaining steps.
  10 Post-deploy monitoring               ← old §9 verbatim.
  11 Troubleshooting (all paths)          ← old §10 + §11.9 + §12.5 merged (QT-03).
  12 Backup & restore runbook (appendix)  ← old §11.1-11.8 → §12.1-§12.8; old §11.9 moved to
                                             §11. Keep all commands verbatim. (QT-05)
Footer "Last updated" line: bump the date, update its § mentions per the mapping.

Env merge rule (§3, QT-02): 3.1 = old §5 core table; 3.2 = old §5 "Phase 6 — Backup & restore
keys" table; 3.3 = old §12.2's rows NOT already covered (MYSQL_ROOT_PASSWORD; the DB-trio →
MARIADB_* mapping note; LOG_CHANNEL=stderr) each tagged "Docker panels (Dokploy/Coolify) only",
plus old §12.2's intro sentence (set once in the panel Environment tab; interpolated into
compose). For keys duplicated across old §5 and old §12.2 (APP_KEY, APP_URL, APP_DEBUG,
MAIL_*, BACKUP_*): UNION the notes from both tables into the single §3 row — no information
loss. §6.2 repeats one pointer line into §3.3.

Troubleshooting merge rule (§11, QT-03): one table, columns Symptom | Likely cause | Fix |
Path. Exactly two de-duplications: "Month-close never completes" (old §10 supervisor row +
old §12.5 queue-service row → ONE row listing both fixes) and "500 on PDF/Excel export"
(old §10 fpm row + old §12.5 container-memory row → ONE row listing both fixes). Every other
row is unique — keep all 20 remaining rows with their traceability tags (T-05-03-05,
T-05-03-01, CLOSE-12, Pitfall 2/4/10, T-06-02-07) and rewrite § refs inside rows per mapping.

§ rewrite mapping — apply to EVERY § reference in DEPLOYMENT.md (intro, tables, callouts,
footer) (QT-06):
  §1, §2            unchanged
  §3/§3.1-3.4  →    §4/§4.1-4.4
  §4/§4.1-4.4  →    §5/§5.1-5.4
  §5           →    §3        (env checklist moved up)
  §6           →    §4.4 (Forge/CDN referent) or §5.5 (certbot referent) — by referent
  §7           →    §5.6
  §8           →    §9; "§8 step 2" → §8.1
  §9           →    §10
  §10          →    §11
  §11, §11.1-11.8 → §12, §12.1-12.8
  §11.9        →    §11
  §12/§12.1/§12.3/§12.4 → §6/§6.1/§6.3/§6.4;  §12.2 → §3.3
  §13          →    §7
One ambiguous ref to resolve by referent: the healthcheck note "( /up — §6 step 1 )" points at
the compose healthcheck, not old §6 HTTPS — re-point it to the healthcheck block in §6.1.

Do NOT touch anything under .planning/ (historical records cite the old numbering
as-of-then by design). No code behavior changes — DEPLOYMENT.md only (QT-08).
  </action>
  <verify>
    <automated>OLD_F=$(grep -c '^```' DEPLOYMENT.md); OLD_R=$(grep -c '^|' DEPLOYMENT.md); for n in $(grep -oE '§[0-9]+(\.[0-9]+)?' DEPLOYMENT.md | sed 's/§//g' | sort -u); do grep -qE "^## ${n}\.|^### ${n}\." DEPLOYMENT.md || echo "DEAD REF: §${n}"; done; grep -cE '^## [0-9]+\.' DEPLOYMENT.md; grep -c '^## 3\. Prepare' DEPLOYMENT.md; grep -c '^## 11\. Troubleshooting' DEPLOYMENT.md; wc -l < DEPLOYMENT.md; echo "baseline fences=$OLD_F rows=$OLD_R"</automated>
  </verify>
  <done>12 `## N.` headings in order 1-12; zero DEAD REF lines; §3 and §11 section greps each == 1; fence count == baseline; table rows ≥ baseline − 8 (only the 2 sanctioned troubleshooting merges + env-table duplicate rows whose notes were unioned); line count 730-800; all four paths self-contained with worker+scheduler inside (QT-01/02/03/04/07).</done>
</task>

<task type="auto">
  <name>Task 2: Re-point external DEPLOYMENT.md § citations</name>
  <files>README.md, SETUP-USAGE-DEPLOY.md, .env.example</files>
  <action>
Three files need edits; one is an explicit no-op. Locate by content (line numbers may drift).
- README.md (~L388): disaster-recovery citation — old ref "DEPLOYMENT.md §11" → new ref "DEPLOYMENT.md §12". Leave L28/L94/L396 (no § refs) untouched.
- SETUP-USAGE-DEPLOY.md L167: two refs in one line — old "DEPLOYMENT.md §12" (Dokploy/Coolify) → "§6"; old "DEPLOYMENT.md §13" (cPanel) → "§7". The "§5.3" on that same line is SETUP's OWN numbering — leave it. L214: old "DEPLOYMENT.md §4.2" → "§5.2". L221: old "DEPLOYMENT.md §4.3" → "§5.3". L5/L198/L412 have no § refs — leave. Do NOT renumber anything else in this file (its internal numbering is untouched).
- .env.example L42: old "§11.9 (Pitfall 2)" → "§11 (Pitfall 2)". L94: old "§11.9" → "§11". L96: old "§11.7" → "§12.7". Comments only — no key/value changes (QT-08).
- config/database.php L68 cites "per DEPLOYMENT.md §2" — §2 is unchanged, so verify it still resolves and touch nothing.
- Do NOT edit files under .planning/ — historical planning records keep their as-of-then numbering.
  </action>
  <verify>
    <automated>grep -c 'DEPLOYMENT\.md §12' README.md; grep -oE 'DEPLOYMENT\.md §(6|7|5\.[23])' SETUP-USAGE-DEPLOY.md | wc -l; grep -c 'DEPLOYMENT\.md §11' .env.example; grep -c 'DEPLOYMENT\.md §12\.7' .env.example</automated>
  </verify>
  <done>Counts in order: 1, 4, 2, 1 — every external DEPLOYMENT.md § citation now points at the correct new section; no other content changed in these files (QT-06).</done>
</task>

<task type="auto">
  <name>Task 3: Cross-file consistency sweep + diff sanity</name>
  <files>(none — verification only; reads DEPLOYMENT.md, README.md, SETUP-USAGE-DEPLOY.md, .env.example)</files>
  <action>
1. Re-run the Task-1 dead-ref sweep over DEPLOYMENT.md; expect zero DEAD REF lines.
2. Confirm the external pointer chain survives: DEPLOYMENT.md §7 (Path D) still cites SETUP-USAGE-DEPLOY.md §5.3, and that target heading still exists in SETUP-USAGE-DEPLOY.md (we did not renumber it).
3. Zero-loss spot check via git diff: DEPLOYMENT.md diff carries the Dockerfile/compose/.dockerignore blocks, nginx vhost, supervisor conf, schedule:run cron line, storage chown/chmod block, and all 20 consolidated troubleshooting rows on the + side; eyeball the full diff for stray content loss (markdown lint-by-eye per repo convention — no test suite; QT-07, QT-08).
4. Confirm blast radius: git status shows exactly the four docs (+ .planning/ quick-task bookkeeping), nothing else.
5. Read the restructured §1 → §3 → §4 → §9 once end-to-end as a newcomer would; confirm the serial flow holds and § numbering is consistent (QT-01, QT-06).
  </action>
  <verify>
    <automated>for n in $(grep -oE '§[0-9]+(\.[0-9]+)?' DEPLOYMENT.md | sed 's/§//g' | sort -u); do grep -qE "^## ${n}\.|^### ${n}\." DEPLOYMENT.md || echo "DEAD REF: §${n}"; done; grep -c '^### 5\.3 Deploy on shared hosting' SETUP-USAGE-DEPLOY.md; grep -c 'SETUP-USAGE-DEPLOY.md §5\.3' DEPLOYMENT.md; git diff --stat; git status --porcelain</automated>
  </verify>
  <done>Zero DEAD REF lines; SETUP §5.3 heading grep == 1 and DEPLOYMENT.md §7's pointer to it == 1; git diff --stat lists exactly the 4 docs; porcelain shows no unexpected paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → reader | Documentation-only change; no runtime surface, no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QT-01 | Information disclosure | DEPLOYMENT.md / .env.example | low | accept | Docs-only restructure; all credential values remain placeholders — Task 3's git diff review confirms no real secret is introduced |
</threat_model>

<verification>
- Task 1 automated block: 12 sequential headings, zero dead § refs, fence/row/line baselines hold
- Task 2 automated block: external citation counts 1 / 4 / 2 / 1
- Task 3 automated block: dead-ref sweep clean, SETUP §5.3 chain intact, diff limited to 4 docs
- Eyeball gates (per AGENTS.md verification style — no test suite): serial flow reads correctly; verbatim blocks intact in the diff
</verification>

<success_criteria>
- DEPLOYMENT.md: 12 sections, serial order, §4-§7 = four self-contained paths, one env reference (§3), one troubleshooting table (§11), backup runbook appendix (§12) — QT-01…QT-05
- Every internal § ref resolves; every external citation (README, SETUP-USAGE-DEPLOY.md, .env.example) re-pointed — QT-06
- Zero content loss; docs-only; no .planning/ rewrites — QT-07, QT-08
</success_criteria>

<output>
Create `.planning/quick/260915-1ab-rearrange-deployment-md-into-a-clear-ser/260915-1ab-SUMMARY.md` when done
</output>
