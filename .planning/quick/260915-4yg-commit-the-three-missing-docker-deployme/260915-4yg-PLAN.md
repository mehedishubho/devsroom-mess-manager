---
phase: 260915-4yg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - docker-compose.yml
  - .dockerignore
autonomous: true
requirements: []   # quick task - no roadmap requirements

must_haves:
  truths:
    - "Dokploy can build the app from the repository: Dockerfile, docker-compose.yml, and .dockerignore exist at repo root, content byte-identical to the three fenced blocks in DEPLOYMENT.md subsection 6.1"
    - "The commit touches exactly these 3 files - no other working-tree changes ride along"
    - "No literal secrets in docker-compose.yml - every credential value is a ${VAR} interpolation filled from the panel environment UI (6.1 / 3.3)"
  artifacts:
    - path: ".dockerignore"
      provides: "Build-context exclusions as reviewed in 6.1 - 13 lines (doc lines 278-290; first line .git, last line tests)"
    - path: "Dockerfile"
      provides: "nginx + PHP-FPM 8.4 supervised single container; node:24-alpine Vite asset stage; mariadb-client for spatie dumps (DUMP_BINARY_PATH=/usr/bin); heredoc'd nginx/supervisord/entrypoint configs; entrypoint honors RUN_MIGRATIONS/OPTIMIZE - 88 lines (doc lines 296-383; first line is the syntax-parser directive, last line is the CMD supervisord line)"
    - path: "docker-compose.yml"
      provides: "4 services (app, queue, scheduler, db=mariadb:11.4) built from x-app-env/x-app-base YAML anchors; named volumes app-storage and db-data; no host ports published (panel reverse proxy reaches app:80 internally) - 83 lines (doc lines 389-471; first line x-app-env anchor, last line db-data)"
  key_links:
    - "docker-compose.yml x-app-base build directive -> repo-root Dockerfile (Dokploy build context = repo root)"
    - "Dockerfile ENV RUN_MIGRATIONS/OPTIMIZE defaults <-> compose app service overriding RUN_MIGRATIONS to true (migrations run via app service on deploy)"
    - "compose db service mariadb:11.4 <-> Dockerfile mariadb-client (mysqldump/mysql binaries on /usr/bin serve spatie backup dumps per 12.1)"
---

<objective>
Create the three Docker deployment files at the repo root - Dockerfile, docker-compose.yml, .dockerignore - with content copied VERBATIM from DEPLOYMENT.md subsection 6.1 ("Commit these three files to the repo root", lines 271-477), then commit exactly those three files.

Purpose: Path C (Docker panels / Dokploy) builds from the cloned repo, and 6.1 states the files must be committed, not pasted. The content is already reviewed and committed in DEPLOYMENT.md; this task only relocates it into real files. Mechanical copy - no redesign, re-commenting, or "improvements".

Output: 3 files at repo root + 1 commit containing exactly those files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Source of truth: DEPLOYMENT.md lines 271-477 ONLY (subsection 6.1). Read exactly that range (e.g. Read with offset 271, limit 207) - do not read other sections of the file or explore the codebase.

Block map (verified against the file on disk):
- .dockerignore - bolded label at line 275; fenced block opens line 277, closes line 291; content = lines 278-290 (13 lines)
- Dockerfile - label at line 293; fenced block opens line 295 (language tag dockerfile), closes line 384; content = lines 296-383 (88 lines)
- docker-compose.yml - label at line 386; fenced block opens line 388 (language tag yaml), closes line 472; content = lines 389-471 (83 lines)
Lines 474-476 are rationale prose (MariaDB-vs-MySQL-8.4 reasoning, no host ports published) - context only, NOT file content.

Environment facts: Windows dev box - docker compose config is NOT available, so validation = byte-exact diff against the committed doc + fence-integrity anchors. Repo has no automated test suite (docs-only convention: verification via git/grep only). The three target files do not exist yet (confirmed) - all three are net-new.

Known content anchors (do not "fix" any of these; they are intentional as reviewed): compose defines 4 services (app/queue/scheduler/db) with every ${VAR} interpolated from the panel environment UI so secrets never live in the file; the Dockerfile installs mariadb-client for the spatie backup dumps; the container entrypoint honors RUN_MIGRATIONS and OPTIMIZE env flags; .dockerignore intentionally excludes tests and .claude.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the three 6.1 blocks verbatim to repo root and commit</name>
  <files>Dockerfile, docker-compose.yml, .dockerignore</files>
  <read_first>DEPLOYMENT.md lines 271-477 only (subsection 6.1). CRITICAL: copy block content from the file on disk - never from a rendered/quoted view of it. The Dockerfile's long continuation and echo lines have been observed to display mangled through tool output, so any copy of the block appearing in chat or tool results is untrustworthy. The file on disk is authoritative.</read_first>
  <action>
    Mechanical verbatim copy per DEPLOYMENT.md 6.1. Do NOT redesign, re-comment, reorder, strip blank lines, add or drop entries, or otherwise improve anything. Byte-exact means: exactly the lines between each block's opening and closing fence, in order, one line each, nothing added or removed, single trailing newline at end of each file, LF line endings, heredoc EOF markers preserved.

    1. Create .dockerignore at repo root containing exactly doc lines 278-290 (13 lines). Anchor: starts with the .git entry, ends with the tests entry.
    2. Create Dockerfile at repo root containing exactly doc lines 296-383 (88 lines). Sanity anchors from 6.1: opens with the docker syntax-parser directive; node:24-alpine assets stage building Vite output; php:8.4-fpm-alpine runtime; apk install line includes nginx, mariadb-client, unzip and supervisor; three COPY-heredocs (nginx default.conf, supervisord.conf, and /entrypoint.sh - the entrypoint gates migrate plus storage:link on RUN_MIGRATIONS and the config/route/view caches on OPTIMIZE); closes with the ENV defaults for those flags, EXPOSE, ENTRYPOINT and CMD lines.
    3. Create docker-compose.yml at repo root containing exactly doc lines 389-471 (83 lines). Sanity anchors from 6.1: x-app-env and x-app-base YAML anchors; services app (runs migrations on deploy, wget healthcheck against the /up route), queue (php artisan queue:work on the database connection), scheduler (php artisan schedule:work), db (mariadb 11.4 image with its healthcheck script); named volumes for app storage and database data; publishes no host ports of any kind - the panel's reverse proxy reaches the app container over the internal Docker network (rationale at line 476).
    4. Run every check in the verify block. All must pass before committing.
    5. Stage and commit ONLY these three files: git add Dockerfile docker-compose.yml .dockerignore, then commit with message: deploy: add Dockerfile, docker-compose.yml, .dockerignore (DEPLOYMENT.md 6.1 verbatim) - ASCII only; if git log --oneline -5 shows a different house style, match it while keeping the 6.1 reference.
  </action>
  <verify>
    <automated>
      From repo root (git-bash):
      (a) Existence + scope: ls Dockerfile docker-compose.yml .dockerignore succeeds. Pre-commit: git status --porcelain shows ONLY the three untracked target files and nothing else. Post-commit: git show --stat HEAD lists exactly 3 files (this is the diff-scope gate - only the 3 files changed).
      (b) Fence integrity - each file starts where its doc block starts and ends where it ends:
          .dockerignore: head -n 1 = .git ; tail -n 1 = tests ; wc -l = 13
          Dockerfile: head -n 1 = # syntax=docker/dockerfile:1 ; tail -n 1 = CMD ["supervisord", "-c", "/etc/supervisord.conf"] ; wc -l = 88
          docker-compose.yml: head -n 1 = x-app-env: &app-env ; tail -n 1 = db-data: ; wc -l = 83
      (c) Byte-exact proof (mechanical stand-in for docker compose config -q, unavailable on this box): sed-extract each doc range and diff against the file; output MUST be empty for all three:
          sed -n '278,290p' DEPLOYMENT.md | diff - .dockerignore
          sed -n '296,383p' DEPLOYMENT.md | diff - Dockerfile
          sed -n '389,471p' DEPLOYMENT.md | diff - docker-compose.yml
          If diff reports only CRLF carriage-return noise, re-run with diff --strip-trailing-cr to confirm content parity; the working-tree files themselves must be LF (the Dokploy build checks out LF on Linux).
      (d) Structure spot-checks (all values come from the committed 6.1 content):
          grep -cE '^  (app|queue|scheduler|db):' docker-compose.yml  -> 4
          grep -c 'mariadb-client' Dockerfile                         -> at least 1
          grep -c 'RUN_MIGRATIONS' Dockerfile                         -> at least 2 (entrypoint gate + ENV default)
          grep -cE '(PASSWORD|_KEY): [^$]' docker-compose.yml         -> 0 (every credential is a ${VAR} interpolation, none literal)
          grep -cE '^[[:space:]]*ports:' docker-compose.yml           -> 0 (nothing published to the host)
    </automated>
  </verify>
  <done>Dockerfile, docker-compose.yml and .dockerignore exist at repo root, each byte-identical to its 6.1 fenced block (empty diff in check c); the HEAD commit touches exactly those 3 files; compose credentials are interpolation-only; no host-port mappings present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> Dokploy build | The committed Dockerfile/compose become the production build and runtime definition on the panel |
| panel env UI -> containers | Secrets are injected at deploy time, never stored in the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q4YG-01 | Tampering | Copied file content drifting from the reviewed 6.1 blocks | medium | mitigate | Byte-exact sed-diff verification (check c) plus head/tail/wc fence anchors (check b); commit constrained to exactly 3 files (check a) |
| T-Q4YG-02 | Information Disclosure | Credential values baked into docker-compose.yml | medium | mitigate | 6.1 design keeps secrets in the panel env UI (3.3); verify grep confirms zero literal secret values (check d) |
| T-Q4YG-03 | Denial of Service | Accidental host-port mapping breaking the panel reverse-proxy design | low | mitigate | Verify grep confirms no host-port publishing in the compose file (check d) |
| T-Q4YG-SC | Tampering | Package/registry installs | N/A | accept | No installs occur in this task; image tags (node:24-alpine, php:8.4-fpm-alpine, composer:2, mariadb:11.4) were already reviewed when DEPLOYMENT.md was committed |
</threat_model>

<verification>
- git show --stat HEAD lists exactly Dockerfile, docker-compose.yml, .dockerignore
- All three sed-vs-file diffs from verify check (c) are empty (byte-identical to DEPLOYMENT.md 6.1)
- All fence-integrity anchors from verify check (b) match
- git status is clean after commit (nothing left behind, nothing else staged)
</verification>

<success_criteria>
- The three files exist at repo root, content verbatim from DEPLOYMENT.md 6.1 (diff-proven, not eyeball-proven)
- One commit containing exactly those 3 files, message referencing 6.1
- Compose file contains no literal secrets and no host-port publishing
- Dokploy can now build from the repository (files committed, not pasted, per 6.1)
</success_criteria>

<output>
Create SUMMARY at .planning/quick/260915-4yg-commit-the-three-missing-docker-deployme/260915-4yg-SUMMARY.md when done, and append a row to the Quick Tasks Completed table in .planning/STATE.md (repo convention).
</output>
