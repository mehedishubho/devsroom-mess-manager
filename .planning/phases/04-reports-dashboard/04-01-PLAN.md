---
phase: 04-reports-dashboard
plan: 01
type: execute
wave: 2
depends_on: ["04-00"]
files_modified:
  - routes/web.php
  - app/Services/ReportService.php
  - app/Services/MemberStatementService.php
  - app/Http/Controllers/Mess/ReportController.php
  - app/Http/Requests/Report/MonthNavigationRequest.php
  - app/Http/Requests/Report/MemberStatementRequest.php
  - app/Http/Requests/Report/ExpenseReportRequest.php
  - app/Http/Requests/Report/PaymentReportRequest.php
  - resources/views/components/month-nav.blade.php
  - resources/views/components/report-toolbar.blade.php
  - resources/views/mess/reports/monthly.blade.php
  - resources/views/mess/reports/member-statement.blade.php
  - resources/views/mess/reports/expenses.blade.php
  - resources/views/mess/reports/payments.blade.php
  - resources/views/mess/reports/_filters/expenses.blade.php
  - resources/views/mess/reports/_filters/payments.blade.php
  - tests/Feature/Report/MonthlyReportTest.php
  - tests/Feature/Report/MemberStatementTest.php
  - tests/Feature/Report/ExpenseReportTest.php
  - tests/Feature/Report/PaymentReportTest.php
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, RPT-04]

must_haves:
  truths:
    - "Manager (role:admin) can open /mess/reports/monthly and see totals (members, meals, meal_rate, total_bazar, total_fixed, total_due, total_advance) plus a per-member table"
    - "Manager can navigate the Monthly Report across months via a ◀ Month ▶ picker (D-20)"
    - "Manager can open /mess/reports/member-statement?member_id=X&year=Y&month=M and see the member's full ledger (meals summary, guest meals, payments, fixed share, opening + closing advance/due) with meal-rate math shown (D-25)"
    - "Manager can open /mess/reports/expenses with date/category/month filters and 'This month' / 'Last month' presets, sticky in URL (D-18)"
    - "Manager can open /mess/reports/payments with member/method/date filters and presets, sticky in URL"
    - "Closed-month path reads MonthlyMemberSummary snapshot; open month reads live via BillPreviewService (D-26)"
    - "Cross-mess member access returns 404 (route-model binding + BelongsToActiveMess scope)"
    - "Every money value renders via App\\Support\\Money::taka() (no bdt())"
    - "Empty report period shows a hint (D-28), not a wall of ৳0.00"
    - "Member (role:user) GET on /mess/reports/* returns 403"
  artifacts:
    - path: "app/Services/ReportService.php"
      provides: "monthlyReport(year,month) with D-26 closed-vs-open switch + expenseReport(filters) + paymentReport(filters) aggregations"
      contains: "BillPreviewService"
    - path: "app/Services/MemberStatementService.php"
      provides: "forMember(memberId,year,month) — wraps BillPreviewService::forMember + adds daily meal breakdown + guest meal rows"
      contains: "dailyBreakdown"
    - path: "app/Http/Controllers/Mess/ReportController.php"
      provides: "monthly(), memberStatement(), expenses(), payments() — thin View-returning actions"
      contains: "class ReportController"
    - path: "app/Http/Requests/Report/ExpenseReportRequest.php"
      provides: "Validates from/to/category_id/month GET filter params (mass-assignment protection)"
      contains: "'from'"
    - path: "app/Http/Requests/Report/PaymentReportRequest.php"
      provides: "Validates member_id/method/from/to GET filter params"
      contains: "'method'"
    - path: "resources/views/components/month-nav.blade.php"
      provides: "Reusable ◀ Month ▶ + dropdown + This-month link component (D-20)"
      contains: "@props"
    - path: "resources/views/mess/reports/monthly.blade.php"
      provides: "Monthly Report HTML view (totals + per-member table)"
      contains: "Money::taka"
  key_links:
    - from: "app/Http/Controllers/Mess/ReportController.php"
      to: "app/Services/ReportService.php"
      via: "Constructor DI"
      pattern: "private readonly ReportService"
    - from: "app/Services/ReportService.php"
      to: "app/Services/BillPreviewService.php"
      via: "preview() / forMember() call for open month"
      pattern: "\\$this->preview->preview\\(|\\$this->preview->forMember\\("
    - from: "routes/web.php"
      to: "ReportController"
      via: "mess.reports.* routes"
      pattern: "mess/reports/monthly"
---

<objective>
Build the 4 manager-side reports (RPT-01..RPT-04) — Monthly Report, Member Statement, Expense Report, Payment Report. HTML only (PDF/Excel come in Plan 4.3). The reports reuse `BillPreviewService` (open month) and `MonthlyMemberSummary` snapshot (closed month, D-26); Expense/Payment reports query their tables directly with validated filters (D-18).

Purpose: This is the reports foundation. Plan 4.2 (member views) reuses `ReportService` and `MemberStatementService`. Plan 4.3 (exports) reuses the controllers' data shape.

Output: 4 report routes + views + supporting services + tests. No PDF/Excel yet (those wire in Plan 4.3).
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@.planning/phases/04-reports-dashboard/04-CONTEXT.md
@.planning/phases/04-reports-dashboard/04-RESEARCH.md
@app/Services/BillPreviewService.php
@app/Http/Controllers/Mess/BillPreviewController.php
@app/Http/Controllers/Controller.php
@routes/web.php
@resources/views/components/mess-date-nav.blade.php
@resources/views/components/tab-nav.blade.php
@resources/views/mess/bill-preview/index.blade.php

<interfaces>
<!-- BillPreviewService return shape — wired into ReportService verbatim -->
From app/Services/BillPreviewService.php:
```php
public function preview(int $year, int $month): array;
// top-level: year, month, total_bazar, total_meals, meal_rate, total_fixed, days_in_month, members[]
// members[] row keys: member_id, name, meals, meal_cost, fixed_share, guest_total,
//   bill, bill_payments, advance_payments, advance_applied (= bill_payments, DO NOT SHOW),
//   due, advance_balance, due_balance, active_days, status
public function forMember(int $memberId, int $year, int $month): ?array;
public function cacheKey(int $messId, int $year, int $month): string;  // "bill-preview:{mess}:{YYYY}-{MM}"
```

<!-- Existing controller pattern (BillPreviewController) — mirror this -->
```php
class BillPreviewController extends Controller
{
    public function __construct(private readonly BillPreviewService $service) {}
    public function index(Request $request): View
    {
        [$year, $month] = $this->resolveYearMonth($request);
        $preview = $this->service->preview($year, $month);
        return view('mess.bill-preview.index', compact('preview', 'year', 'month'));
    }
}
```

<!-- Money helper (Gap 1 resolution: standardize on Money::taka, NOT bdt) -->
```php
\App\Support\Money::taka($value);  // returns "৳1,234.00"
```

<!-- D-26 switch reference: MonthlyClosing + MonthlyMemberSummary for closed month -->
MonthlyClosing: where('mess_id', $messId)->where('year', $y)->where('month', $m)->first()
MonthlyMemberSummary: where('monthly_closing_id', $closing->id)->get() — per-member snapshot rows.

<!-- Existing component pattern (mess-date-nav is day-scoped; month-nav is month-scoped) -->
resources/views/components/mess-date-nav.blade.php uses @props(['date', 'route' => 'mess.meals.index']) + Carbon arithmetic. Mirror this for month-nav.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReportService + MemberStatementService + Form Requests + month-nav component</name>
  <files>app/Services/ReportService.php, app/Services/MemberStatementService.php, app/Http/Requests/Report/MonthNavigationRequest.php, app/Http/Requests/Report/MemberStatementRequest.php, app/Http/Requests/Report/ExpenseReportRequest.php, app/Http/Requests/Report/PaymentReportRequest.php, resources/views/components/month-nav.blade.php, tests/Feature/Report/MonthlyReportTest.php, tests/Feature/Report/MemberStatementTest.php</files>
  <read_first>
    - app/Services/BillPreviewService.php (use the exact field names — do NOT re-derive)
    - app/Models/MonthlyClosing.php (closed-month snapshot lookup signature)
    - app/Models/MonthlyMemberSummary.php (per-member snapshot row shape)
    - app/Models/Expense.php, app/Models/ExpenseCategory.php, app/Models/Payment.php (table columns for filter queries)
    - app/Support/ExpenseKind.php, app/Support/PaymentType.php (enum values for filters)
    - app/Models/MealEntry.php, app/Models/GuestMeal.php (for daily breakdown in MemberStatementService)
    - app/Models/Member.php (status, joining_date, leaving_date)
    - resources/views/components/mess-date-nav.blade.php (pattern to mirror for month-nav)
    - app/Http/Requests/My/StoreMealOffRequest.php or any existing Form Request (form-request convention)
    - database/migrations/2026_06_16_221100_create_expense_categories_table.php (kind column)
    - database/migrations/2026_06_16_221500_create_monthly_member_summaries_table.php (snapshot columns)
  </read_first>
  <behavior>
    <!-- RED phase — write these test cases FIRST, watch them fail, then implement. -->
    - MonthlyReportTest:: test_guest_is_redirected_to_login (GET /mess/reports/monthly without auth → 302 to /login)
    - MonthlyReportTest:: test_member_role_forbidden (logged-in role:user → 403)
    - MonthlyReportTest:: test_manager_sees_totals_and_member_table (role:admin; assert response 200, sees __('Meal rate'), sees Money::taka(meal_rate) value, sees at least one member name)
    - MonthlyReportTest:: test_month_picker_query_param_changes_period (GET ?year=2026&month=5 → response sees "May 2026" string in period label)
    - MonthlyReportTest:: test_closed_month_uses_snapshot (factory a MonthlyClosing + a MonthlyMemberSummary for 2026-04; GET ?year=2026&month=4 → assert response mentions snapshot-derived total, not re-computed)
    - MemberStatementTest:: test_manager_views_active_mess_member (GET /mess/reports/member-statement?member_id=X&year=Y&month=M → 200, sees meal-rate math line, sees daily breakdown table)
    - MemberStatementTest:: test_manager_cannot_view_cross_mess_member (factory a Member in mess_id=2; GET with that member_id → 404)
    - MemberStatementTest:: test_member_role_forbidden (role:user → 403)
  </behavior>
  <action>
1. **Form Requests** — create 4 Form Request classes under `app/Http/Requests/Report/`. Use the `rules()` method (Laravel 13 attribute-based style or classic — match existing Form Requests like `StoreMealOffRequest`). All four authorize `true` (route middleware enforces `role:admin`). Rules:

   - **MonthNavigationRequest**: `'year' => ['sometimes', 'integer', 'min:2000', 'max:2100'], 'month' => ['sometimes', 'integer', 'min:1', 'max:12']`
   - **MemberStatementRequest**: same year/month rules as above + `'member_id' => ['required', 'integer', 'exists:members,id']`. (The cross-mess check happens via `BelongsToActiveMess` global scope on route-model binding OR via the service using `Member::query()` which auto-scopes — verify by reading `app/Models/Member.php` for the scope registration. If the global scope is applied, the 404 happens at the Eloquent lookup, NOT the validation. To be safe, in the controller use `Member::where('id', $id)->firstOrFail()` which goes through MessScope.)
   - **ExpenseReportRequest**: `'from' => ['nullable', 'date'], 'to' => ['nullable', 'date', 'after_or_equal:from'], 'category_id' => ['nullable', 'integer', 'exists:expense_categories,id'], 'month' => ['nullable', 'string', 'regex:/^\d{4}-\d{2}$/'], 'preset' => ['nullable', 'string', 'in:this,last']`
   - **PaymentReportRequest**: `'member_id' => ['nullable', 'integer', 'exists:members,id'], 'method' => ['nullable', 'string', 'in:cash,bkash,nagad,rocket,bank'], 'from' => ['nullable', 'date'], 'to' => ['nullable', 'date', 'after_or_equal:from'], 'preset' => ['nullable', 'string', 'in:this,last']`
     - IMPORTANT: Read the actual `Payment` model + migration to get the exact `method` enum/cast values. The list above is from REQUIREMENTS PAY-02; verify against `app/Models/Payment.php` and `database/migrations/2026_06_16_221300_create_payments_table.php` and adjust the `'in:'` clause to match the real stored values.

2. **ReportService** (`app/Services/ReportService.php`) — constructor-inject `BillPreviewService`. Methods:

   - `monthlyReport(int $year, int $month): array` — D-26 switch:
     ```php
     $messId = \App\Models\Mess::activeId();
     $closing = \App\Models\MonthlyClosing::query()
         ->where('mess_id', $messId)->where('year', $year)->where('month', $month)->first();
     if ($closing) {
         return $this->monthlyFromSnapshot($closing, $year, $month);
     }
     return $this->preview->preview($year, $month);
     ```
     `monthlyFromSnapshot` reads `MonthlyMemberSummary::where('monthly_closing_id', $closing->id)->get()` and assembles the SAME top-level shape as `BillPreviewService::preview()` returns (year, month, total_bazar, total_meals, meal_rate, total_fixed, days_in_month, members[]). Map snapshot columns to the preview's member-row keys verbatim (read `monthly_member_summaries` migration for exact column names — they should mirror: meals, meal_cost, fixed_share, bill, bill_payments, due, advance_balance, due_balance, etc.). Include a `'source' => 'snapshot' | 'live'` key on the top-level array so the view can show a closed-month badge (D-24 / open question #5).

   - `expenseReport(array $filters): array` — returns `['rows' => LengthAwarePaginator, 'totals' => ['amount' => float]]`:
     ```php
     $query = \App\Models\Expense::query()
         ->with(['category:id,name,kind', 'member:id,name'])
         ->where('mess_id', \App\Models\Mess::activeId())
         ->when($filters['from'] ?? null, fn($q,$d) => $q->where('date', '>=', $d))
         ->when($filters['to']   ?? null, fn($q,$d) => $q->where('date', '<=', $d))
         ->when($filters['category_id'] ?? null, fn($q,$id) => $q->where('expense_category_id', $id))
         ->when($filters['month'] ?? null, function($q,$m) {
             [$y, $mo] = explode('-', $m);
             $q->whereYear('date', $y)->whereMonth('date', $mo);
         })
         ->orderBy('date', 'desc');
     $rows = $query->paginate(50)->appends(request()->query());
     $totals = ['amount' => (float) (clone $query)->sum('amount')];
     return compact('rows', 'totals');
     ```

   - `paymentReport(array $filters): array` — same shape as expenseReport, query on `Payment` with filters for member_id, method, from, to, month (year/month extract). Eager-load `member:id,name` and `enteredBy:id,name`. Order by date desc, paginate(50), compute totals['amount'] = sum.

3. **MemberStatementService** (`app/Services/MemberStatementService.php`) — constructor-inject `BillPreviewService`. Method:

   ```php
   public function forMember(int $memberId, int $year, int $month): array
   ```
   Returns:
   - `'row'` — the member's preview row (from `$this->preview->forMember($memberId, $year, $month)` for open month; from `MonthlyMemberSummary` for closed month — same D-26 switch as ReportService::monthlyReport)
   - `'daily'` — array of `['date' => 'YYYY-MM-DD', 'breakfast' => bool, 'lunch' => bool, 'dinner' => bool, 'meal_value' => float]` from `MealEntry::where('member_id', $memberId)->whereBetween('date', [$start, $end])->orderBy('date')->get()`. meal_value per row = breakfast?MealType::value(BREAKFAST):0 + lunch?MealType::value(LUNCH):0 + dinner?MealType::value(DINNER):0
   - `'guests'` — `GuestMeal::where('member_id', $memberId)->whereBetween('date', [$start, $end])->orderBy('date')->get()`
   - `'payments'` — `Payment::where('member_id', $memberId)->whereBetween('date', [$start, $end])->orderBy('date')->get()`
   - `'is_closed'` — bool (MonthlyClosing exists for this mess/year/month)
   - `'period_label'` — D-24: if `$is_closed` → `Carbon::create($y,$m,1)->translatedFormat('F Y')`; else `__('As of today, :date', ['date' => now()->translatedFormat('j F Y')])`
   - `'source'` — `'snapshot' | 'live'`

4. **`<x-month-nav>` component** (`resources/views/components/month-nav.blade.php`) — mirror `mess-date-nav.blade.php` structure exactly but month-scoped. Props: `['year', 'month', 'route' => 'mess.reports.monthly']`. Render: ◀ arrow (subMonth), `<select>` dropdown listing last 24 months (`now()->subMonths(23)..now()` formatted as 'F Y', value = `route($route, ['year'=>$y, 'month'=>$m])`), ▶ arrow (addMonth), and a "This month" link. All anchors use `min-h-[44px]` and the same Tailwind classes as `mess-date-nav`. (Use the exact Blade/CSS code from RESEARCH Pattern "Month nav".)

5. **Tests** — write `tests/Feature/Report/MonthlyReportTest.php` and `tests/Feature/Report/MemberStatementTest.php` covering the behaviors above. Use `RefreshDatabase`, factory `Member`, `Mess`, `Expense`, `MealEntry`, `MonthlyClosing`, `MonthlyMemberSummary` as needed. Assert via `$this->actingAs($admin)->get(route('mess.reports.monthly', ['year'=>2026,'month'=>5]))`. Verify the meal-rate math line text appears: assert the response content contains `__('Meal rate')`.

6. Run `vendor/bin/pint --test app/ tests/` — must be clean. Run `php artisan test --filter=Report` — RED first, GREEN after implementation.
  </action>
  <verify>
    <automated>php artisan test --filter=Report && vendor/bin/pint --test app/Services/ReportService.php app/Services/MemberStatementService.php app/Http/Requests/Report/ app/Http/Controllers/Mess/ReportController.php tests/Feature/Report/</automated>
  </verify>
  <acceptance_criteria>
    - `app/Services/ReportService.php` exists and contains `class ReportService` and the literal `BillPreviewService` (DI)
    - `app/Services/ReportService.php` contains the literal `MonthlyClosing::query()` (D-26 closed-month path)
    - `app/Services/MemberStatementService.php` contains `dailyBreakdown` OR `->where('member_id', $memberId)` for MealEntry (daily breakdown source)
    - All 4 Form Requests exist under `app/Http/Requests/Report/`
    - `app/Http/Requests/Report/ExpenseReportRequest.php` contains `'from'` and `'category_id'` rule keys
    - `app/Http/Requests/Report/PaymentReportRequest.php` contains `'method'` rule key
    - `resources/views/components/month-nav.blade.php` exists with `@props(['year', 'month'` declaration
    - `tests/Feature/Report/MonthlyReportTest.php` exits 0 and contains at least 5 `test_` methods
    - `tests/Feature/Report/MemberStatementTest.php` exits 0 and contains `test_manager_cannot_view_cross_mess_member`
    - `php artisan test --filter=Report` exits 0
    - `vendor/bin/pint --test` (on the listed files) exits 0
    - No file under `app/` created in this task contains the string `bdt(` (must use `Money::taka(`)
  </acceptance_criteria>
  <done>
    ReportService + MemberStatementService + 4 Form Requests + month-nav component exist and pass tests. D-26 closed/open switch is implemented. Daily breakdown for member statement works. Money uses Money::taka() everywhere.
  </done>
</task>

<task type="auto">
  <name>Task 2: ReportController + routes + 4 report views + filter partials</name>
  <files>app/Http/Controllers/Mess/ReportController.php, routes/web.php, resources/views/components/report-toolbar.blade.php, resources/views/mess/reports/monthly.blade.php, resources/views/mess/reports/member-statement.blade.php, resources/views/mess/reports/expenses.blade.php, resources/views/mess/reports/payments.blade.php, resources/views/mess/reports/_filters/expenses.blade.php, resources/views/mess/reports/_filters/payments.blade.php</files>
  <read_first>
    - app/Http/Controllers/Mess/BillPreviewController.php (controller pattern — DI + resolveYearMonth)
    - app/Http/Controllers/Controller.php (base class)
    - routes/web.php (find the existing `role:admin + EnsureMessExists` group at line ~44; insert reports routes inside it)
    - resources/views/mess/bill-preview/index.blade.php (view pattern — `@extends('layouts.app')`, `@section('content')`, Money::taka usage)
    - resources/views/components/mess-date-nav.blade.php (component invocation pattern `<x-mess-date-nav :date="$date" />`)
    - resources/views/components/empty-state.blade.php (existing empty-state component — reuse for D-28)
    - app/Services/ReportService.php (just created in Task 1 — invoke its methods)
    - app/Services/MemberStatementService.php (just created in Task 1)
    - app/Models/Member.php (to know ->name accessor + active scope)
    - app/Models/Expense.php (columns: date, amount, description, vendor, receipt_path, expense_category_id, entered_by)
    - app/Models/Payment.php (columns: date, amount, method, type, reference, notes, member_id, entered_by)
  </read_first>
  <action>
1. **ReportController** (`app/Http/Controllers/Mess/ReportController.php`). Mirror `BillPreviewController`. Constructor-inject `ReportService`, `MemberStatementService`. Methods (all return `Illuminate\View\View`):

   - `monthly(MonthNavigationRequest $request)`: `$year = $request->integer('year', now()->year); $month = $request->integer('month', now()->month); $data = $this->reports->monthlyReport($year, $month); $mess = \App\Models\Mess::findOrFail(\App\Models\Mess::activeId()); return view('mess.reports.monthly', compact('data', 'year', 'month', 'mess'));`
   - `memberStatement(MemberStatementRequest $request)`: `$member = \App\Models\Member::where('id', $request->integer('member_id'))->firstOrFail();` (this triggers MessScope 404 for cross-mess); `$year = ...; $month = ...; $statement = $this->statements->forMember($member->id, $year, $month); $members = \App\Models\Member::where('mess_id', Mess::activeId())->whereIn('status', ['active','former'])->orderBy('name')->get(['id','name']);` (member-picker per open question #2); return `view('mess.reports.member-statement', compact('statement','member','members','year','month'));`
   - `expenses(ExpenseReportRequest $request)`: `$filters = $request->validated(); $report = $this->reports->expenseReport($filters); $categories = \App\Models\ExpenseCategory::where('mess_id', Mess::activeId())->orderBy('name')->get(['id','name']); return view('mess.reports.expenses', compact('report','filters','categories'));`
   - `payments(PaymentReportRequest $request)`: `$filters = $request->validated(); $report = $this->reports->paymentReport($filters); $members = \App\Models\Member::where('mess_id', Mess::activeId())->orderBy('name')->get(['id','name']); return view('mess.reports.payments', compact('report','filters','members'));`

2. **Routes** — inside the existing `Route::middleware(['auth', 'role:admin', EnsureMessExists::class])` group in `routes/web.php`, AFTER the existing `mess/bill-preview` route and BEFORE `mess/close` (to group with reporting-adjacent features), add:

   ```php
   Route::prefix('mess/reports')->name('mess.reports.')->group(function () {
       Route::get('monthly', [ReportController::class, 'monthly'])->name('monthly');
       Route::get('member-statement', [ReportController::class, 'memberStatement'])->name('member-statement');
       Route::get('expenses', [ReportController::class, 'expenses'])->name('expenses');
       Route::get('payments', [ReportController::class, 'payments'])->name('payments');
   });
   ```
   Add `use App\Http\Controllers\Mess\ReportController;` to the imports at the top of `routes/web.php`.

3. **`<x-report-toolbar>` component** (`resources/views/components/report-toolbar.blade.php`) — props: `['route', 'year', 'month', 'showExports' => false]`. Renders a toolbar with the month-nav (if year/month provided) AND placeholder export buttons (the actual PDF/Excel hrefs are wired in Plan 4.3; for now show disabled-greyed buttons labeled "PDF" and "Excel" with `aria-disabled="true"`). Use the existing button styles. This keeps the view layer ready for Plan 4.3 wiring.

4. **Monthly Report view** (`resources/views/mess/reports/monthly.blade.php`):
   - `@extends('layouts.app')` + `@section('content')`
   - Page title: `__('Monthly Report')` + closed-month badge if `$data['source'] === 'snapshot'` (small badge text `__('Closed month')`)
   - `<x-month-nav :year="$year" :month="$month" route="mess.reports.monthly" />`
   - `<x-report-toolbar route="mess.reports.monthly" :year="$year" :month="$month" showExports="true" />`
   - Totals grid (responsive 2/3/6 cols): Total Members (count of `$data['members']`), Total Meals (`Money::taka($data['total_meals'])` — wait, meals aren't money; display as `number_format($data['total_meals'], 1)`), Meal Rate `Money::taka($data['meal_rate']) . ' / ' . __('meal')` (D-29: if `$data['meal_rate'] == 0.0` show `Money::taka(0) . ' / ' . __('meal') . ' — ' . __('no bazar recorded yet')`), Total Bazar `Money::taka($data['total_bazar'])`, Total Fixed `Money::taka($data['total_fixed'])`, Total Due `Money::taka(collect($data['members'])->sum('due'))`, Total Advance `Money::taka(collect($data['members'])->sum('advance_balance'))`
   - Per-member table: columns = Member, Status (status-pill), Meals, Meal Cost, Fixed, Guest, Bill, Paid, Due, Advance. All money via `Money::taka()`. Use `<tbody>@foreach ($data['members'] as $row)</tbody>`. Mobile: wrap in `overflow-x-auto`.
   - Empty state (D-28): if `empty($data['members'])` show `<x-empty-state>` with `__('No data for :month yet', ['month' => Carbon::create($year,$month,1)->translatedFormat('F Y')])`. Do NOT render the totals wall of ৳0.00.

5. **Member Statement view** (`resources/views/mess/reports/member-statement.blade.php`):
   - Page title: `$member->name` + ` — ` + `$statement['period_label']` (D-24)
   - Filter bar: a `<form method="GET">` with `<select name="member_id">` (the `$members` collection, `@selected($member->id)`), `<x-month-nav>` for year/month (adapt: pass year/month via hidden inputs synced with month-nav — simplest: render month-nav separately and submit member_id + year + month together via the form's hidden inputs OR via month-nav's anchors). Use the form-with-sticky-querystring pattern from RESEARCH Pattern "Filter UX".
   - Meal-rate math line (D-25): `Money::taka($statement['row']['meal_cost'] / max($statement['row']['meals'], 0.01))` × `number_format($statement['row']['meals'], 1)` `= Money::taka($statement['row']['meal_cost'])`. Wrap in a `<div class="...">` styled clearly.
   - Daily breakdown table (D-23): columns Date (d-m-Y), B, L, D, Meal Value. Iterate `$statement['daily']`. Per-type + grand totals row at the bottom.
   - Guest meals section (only if `$statement['guests']->isNotEmpty()`): table with date, description/notes, charge_amount via `Money::taka()`.
   - Payments section (always show): split into "Bill payments" (filter `type === 'bill_payment'`) and "Advance deposits" (`type === 'advance_deposit'`). Each: date, method (method-pill component), amount via `Money::taka()`, reference.
   - Closing summary card: Opening Advance `Money::taka($statement['row']['advance_balance'])`, Opening Due `Money::taka($statement['row']['due_balance'])`, Fixed Share `Money::taka($statement['row']['fixed_share'])`, Meal Cost `Money::taka($statement['row']['meal_cost'])`, Bill `Money::taka($statement['row']['bill'])`, Paid `Money::taka($statement['row']['bill_payments'])`, Closing Due `Money::taka($statement['row']['due'])`.
   - **DO NOT display `$statement['row']['advance_applied']`** (Pitfall 3 — it equals bill_payments; showing both is confusing + misleading).
   - `<x-report-toolbar>` for export buttons (disabled in this plan).

6. **Expense Report view** (`resources/views/mess/reports/expenses.blade.php`) + **filter partial** (`resources/views/mess/reports/_filters/expenses.blade.php`):
   - Filter partial: GET form with from (date input), to (date input), category_id (select from `$categories`), Apply button, "This month" preset link (`route('mess.reports.expenses', ['from'=>now()->startOfMonth()->toDateString(), 'to'=>now()->endOfMonth()->toDateString()])`), "Last month" preset link. All inputs pre-filled from `request()->query('from')` etc. (sticky, D-18).
   - Main view: title `__('Expense Report')` + total summary at top `Money::taka($report['totals']['amount'])`. Table: Date, Category, Description, Vendor, Amount (via Money::taka), Purchaser/Entered-by. Wrap in `overflow-x-auto`. Use `$report['rows']->links()` for pagination links, with `->appends(request()->query())` to keep filters sticky (the service already calls appends; double-check the view renders the links).
   - Empty state (D-28): if `$report['rows']->isEmpty()` show `<x-empty-state>` with hint `__('No expenses match the current filters.')`.

7. **Payment Report view** (`resources/views/mess/reports/payments.blade.php`) + **filter partial** (`resources/views/mess/reports/_filters/payments.blade.php`):
   - Filter partial: GET form with member_id (select from `$members`), method (select — values must match the actual `method` column values; verify by reading the Payment migration), from, to, Apply, This month, Last month presets.
   - Main view: title `__('Payment Report')`, total summary `Money::taka($report['totals']['amount'])`, table: Date, Member, Method (method-pill), Type (payment-type-pill), Amount, Reference. Pagination + appends.

8. Run `php artisan view:clear && php artisan route:clear`. Run `vendor/bin/pint --test`. Run `php artisan test --filter=Report`.
  </action>
  <verify>
    <automated>php artisan route:list --name=mess.reports | grep -E "(monthly|member-statement|expenses|payments)" && php artisan test --filter=Report && vendor/bin/pint --test app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `app/Http/Controllers/Mess/ReportController.php` contains `class ReportController` and methods `monthly`, `memberStatement`, `expenses`, `payments`
    - `routes/web.php` contains the literal `Route::prefix('mess/reports')->name('mess.reports.')` block with all 4 GET routes
    - `php artisan route:list --name=mess.reports.monthly` succeeds (route is registered)
    - `resources/views/mess/reports/monthly.blade.php` exists and contains the literal `Money::taka(` and the literal `__('Monthly Report')` and `<x-month-nav`
    - `resources/views/mess/reports/member-statement.blade.php` contains the literal `__('Meal rate')` (D-25 math line) and `Money::taka(`
    - `resources/views/mess/reports/member-statement.blade.php` does NOT contain `advance_applied` (Pitfall 3)
    - `resources/views/mess/reports/expenses.blade.php` exists + includes the `_filters/expenses` partial
    - `resources/views/mess/reports/payments.blade.php` exists + includes the `_filters/payments` partial
    - `resources/views/components/report-toolbar.blade.php` exists with `@props` declaration
    - None of the new view files contain the string `bdt(` (must use `Money::taka(`)
    - `php artisan test --filter=Report` exits 0
    - `vendor/bin/pint --test app/ tests/` exits 0
  </acceptance_criteria>
  <done>
    All 4 manager reports render: Monthly Report (with per-member table + month picker), Member Statement (with daily breakdown + meal-rate math), Expense Report (filtered), Payment Report (filtered). Money via Money::taka() everywhere. Cross-mess access returns 404. Member role returns 403. Empty states show hints. Closed-month badge works.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ExpenseReportTest + PaymentReportTest feature tests</name>
  <files>tests/Feature/Report/ExpenseReportTest.php, tests/Feature/Report/PaymentReportTest.php</files>
  <read_first>
    - tests/Feature/Report/MonthlyReportTest.php (pattern for actingAs + assertions, created in Task 1)
    - database/factories/ExpenseFactory.php (or whatever the factory is named — check `database/factories/`)
    - database/factories/PaymentFactory.php
    - database/factories/MemberFactory.php
    - app/Models/Expense.php + app/Models/Payment.php (to know factory-required fields)
    - app/Support/PaymentType.php + app/Support/ExpenseKind.php (enum values used by factories)
  </read_first>
  <behavior>
    <!-- RED phase first -->
    - ExpenseReportTest:: test_manager_sees_expenses_list (factory 3 expenses for active mess; GET /mess/reports/expenses → 200, sees each expense's description + Money::taka amount)
    - ExpenseReportTest:: test_date_filter_sticky_in_url (GET ?from=2026-06-01&to=2026-06-15 → response's filter inputs have those values selected; only matching expenses shown)
    - ExpenseReportTest:: test_category_filter (factory expenses in 2 categories; filter by category_id=X → only X's expenses shown)
    - ExpenseReportTest:: test_this_month_preset (click the preset → URL becomes ?from=first-of-month&to=last-of-month)
    - ExpenseReportTest:: test_member_role_forbidden (role:user GET → 403)
    - ExpenseReportTest:: test_empty_state_when_no_match (filter to a date range with no expenses → response contains the empty-state hint)
    - PaymentReportTest:: test_manager_sees_payments_list (factory 3 payments; 200, sees each)
    - PaymentReportTest:: test_method_filter_respected (2 payments with different methods; filter by method=cash → only cash payments shown)
    - PaymentReportTest:: test_member_filter (filter by member_id → only that member's payments)
    - PaymentReportTest:: test_totals_sum_correct (3 payments summing to X → response shows Money::taka(X) in totals)
    - PaymentReportTest:: test_member_role_forbidden
  </behavior>
  <action>
Write `tests/Feature/Report/ExpenseReportTest.php` and `tests/Feature/Report/PaymentReportTest.php` using `RefreshDatabase` + factory setup. For each test:
1. Factory a `Mess` (or use the existing test helper if Phase 2/3 introduced one — read any prior `tests/Feature/Report/*` or `tests/Feature/Mess/*` test to find the helper).
2. Factory a manager `User` with role `admin` (via Tyro's role assignment — read an existing test like `tests/Feature/Feature/MonthCloseTest.php` or any auth-using test for the exact pattern).
3. Factory domain rows (`Expense` / `Payment`) attached to the active mess + member.
4. `$response = $this->actingAs($manager)->get(route('mess.reports.expenses', ['from' => ..., 'to' => ...]));`
5. Assertions: `$response->assertOk(); $response->assertSee(__('Expense Report')); $response->assertSee($expense->description);`

Verify the totals math: factory 3 payments with amounts 100.00, 200.50, 300.25; assert response contains `Money::taka(600.75)` = "৳600.75".

Run `php artisan test --filter=Report`. Run `vendor/bin/pint --test tests/`.
  </action>
  <verify>
    <automated>php artisan test --filter='ExpenseReportTest|PaymentReportTest' && vendor/bin/pint --test tests/Feature/Report/</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/Report/ExpenseReportTest.php` exits 0 and contains at least 6 `test_` methods
    - `tests/Feature/Report/PaymentReportTest.php` exits 0 and contains at least 6 `test_` methods
    - Both test files contain `test_member_role_forbidden` asserting 403
    - Both test files use `$this->actingAs(` (not raw `$this->get`)
    - `php artisan test --filter=Report` exits 0 (all Report tests green)
    - `vendor/bin/pint --test tests/` exits 0
  </acceptance_criteria>
  <done>
    Expense + Payment report filters tested: sticky querystring, presets, totals, empty states, role enforcement.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /mess/reports/* | Manager GET requests cross here. Filter params (member_id, category_id, method, from, to) are user-controlled. |
| Route-model binding | `{member}` / `member_id` could reference a member outside the active mess. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Elevation of privilege | /mess/reports/* routes | mitigate | Routes are inside `['auth', 'role:admin', EnsureMessExists::class]` middleware group — `role:user` returns 403. Test: MonthlyReportTest::test_member_role_forbidden + MemberStatementTest::test_member_role_forbidden. |
| T-04-01-02 | Information disclosure (cross-mess) | member_id query param | mitigate | Controller does `Member::where('id', $id)->firstOrFail()` — MessScope global scope auto-filters by active mess_id, returning 404 for any cross-mess member. Test: MemberStatementTest::test_manager_cannot_view_cross_mess_member asserts 404. |
| T-04-01-03 | Tampering / mass-assignment | filter query params | mitigate | All filters pass through Form Requests (`ExpenseReportRequest`, `PaymentReportRequest`, `MemberStatementRequest`, `MonthNavigationRequest`) with strict rules — no `$request->all()`. Date format validated; enums (`method`, `preset`) constrained by `in:`. |
| T-04-01-04 | Information disclosure | `advance_applied` field | mitigate | Member Statement view omits `advance_applied` entirely (Pitfall 3). Reports show only `bill_payments`, `advance_payments`, `advance_balance`, `due_balance`. Verified by acceptance criterion: view must NOT contain `advance_applied`. |
| T-04-01-05 | Information disclosure | aggregates shown to members | accept (for this plan) | This plan only builds manager routes (role:admin). Member-side aggregates-only Monthly Report is Plan 4.2 with explicit D-19 enforcement (no per-member table). |
| T-04-01-06 | Denial of service | unbounded expense/payment queries | mitigate | Queries are paginated (50 rows/page via `->paginate(50)`). Totals use `sum()` (cheap, indexed). Form Requests validate `to >= from` (after_or_equal) and the `preset` is constrained — large ranges are bounded by user intent. No export endpoints in this plan (those land in Plan 4.3 with chunking). |
</threat_model>

<verification>
- `php artisan test --filter=Report` exits 0 (MonthlyReportTest, MemberStatementTest, ExpenseReportTest, PaymentReportTest)
- `php artisan route:list --name=mess.reports` lists all 4 routes
- Manual (optional, after test pass): `php artisan serve` + log in as admin + visit each report URL; verify filter presets work
- `vendor/bin/pint --test app/ tests/` clean (style gate)
- Full suite regression: `php artisan test` (154 prior + new Report tests)
</verification>

<success_criteria>
- All 4 manager reports render at /mess/reports/{monthly,member-statement,expenses,payments} (RPT-01..RPT-04)
- Monthly Report shows totals + per-member table + month picker
- Member Statement shows daily breakdown + meal-rate math (D-25) + 8-section ledger (D-22)
- Expense + Payment reports have sticky filters + This-month/Last-month presets (D-18)
- D-26 closed-month vs open-month switch implemented in ReportService + MemberStatementService
- Cross-mess member access returns 404 (test verifies)
- Member role returns 403 on all 4 routes (tests verify)
- Money displayed via Money::taka() (no bdt() anywhere)
- Empty states show hints (D-28); zero meal rate shows hint (D-29)
- Closed-month badge shows next to period label
- All Report tests pass; Pint clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-reports-dashboard/04-01-manager-reports-SUMMARY.md`
</output>
