---
phase: 05-polish-pilot
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - README.md
  - AGENTS.md
  - DEPLOYMENT.md
  - .planning/phases/04-reports-dashboard/04-HUMAN-UAT.md
  - .planning/phases/05-polish-pilot/05-PILOT-RUNBOOK.md
  - .planning/phases/05-polish-pilot/05-VERIFICATION.md
autonomous: false
requirements:
  - PERF-13
requirements_cross_plan:
  - PERF-04   # mobile real-device verification happens in the pilot (D-11)
  - PERF-06   # cache refresh confirmed in pilot (HUMAN-UAT item #4)

user_setup:
  - service: "Pilot VPS (DigitalOcean/Hetzner/etc.)"
    why: "Host the production deployment for the one real mess pilot (D-15). Forge (managed) or manual setup."
    env_vars:
      - name: "Production MySQL creds (DB_HOST/PORT/DATABASE/USERNAME/PASSWORD)"
        source: "VPS MySQL provision — set at deploy time"
      - name: "APP_URL (public HTTPS URL)"
        source: "DNS / Forge site config"
      - name: "APP_DEBUG=false"
        source: "Hard-coded in production .env per DEPLOYMENT.md checklist"
    dashboard_config:
      - task: "Provision VPS + install supervisor for queue worker"
        location: "VPS ssh (manual) or Forge UI"
      - task: "Configure schedule:run cron"
        location: "Forge Scheduler UI or `crontab -e` on the VPS"
  - service: "Pilot mess access"
    why: "D-01 — a real mess the dev has direct access to (own/family/close contact). The pilot cannot run without this."
    human_action: "The dev configures mess + members + settings (hybrid onboarding per D-03); the manager runs daily ops. Direct WhatsApp feedback channel per D-05."

must_haves:
  truths:
    - "README.md is a full rewrite (no Laravel stub) with: what the app is, prerequisites, setup, demo seeder, demo creds, common commands (D-16)"
    - "AGENTS.md has a refreshed auto-gen map AND a hand-written Domain Walkthrough section covering bill math, month-close flow, cache key strategy, role/IDOR model (D-17)"
    - "DEPLOYMENT.md is a production-hardening checklist: APP_DEBUG=false, HTTPS, APP_URL, supervisor worker, schedule:run cron, storage perms, prod MySQL .env (D-18, D-15)"
    - "The 4 Phase 4 HUMAN-UAT items (chart rendering, PDF layout, mobile responsive, cache refresh) are cleared with human-confirmed results (D-23)"
    - "The pilot is prepared: deploy target documented, demo dataset seeded, hybrid-onboarding runbook written (D-03, D-15)"
    - "The pilot month-close is run on a real mess and members view their own bills (success #12 — HUMAN, autonomous: false)"
  artifacts:
    - path: "README.md"
      provides: "Full project README replacing the Laravel stub"
      contains: "## Installation"
    - path: "AGENTS.md"
      provides: "Refreshed agent map + hand-written domain walkthrough"
      contains: "Domain Walkthrough"
    - path: "DEPLOYMENT.md"
      provides: "Production hardening checklist + Forge/manual VPS paths"
      contains: "APP_DEBUG=false"
    - path: ".planning/phases/05-polish-pilot/05-PILOT-RUNBOOK.md"
      provides: "Step-by-step pilot onboarding + month-close runbook"
      contains: "Hybrid Onboarding"
    - path: ".planning/phases/04-reports-dashboard/04-HUMAN-UAT.md"
      provides: "All 4 HUMAN-UAT items marked PASS with result entries"
      contains: "pass"
  key_links:
    - from: "README.md"
      to: "Plan 01's PerfDemoSeeder"
      via: "demo creds: manager@demo.test / member@demo.test"
      pattern: "manager@demo\\.test"
    - from: "DEPLOYMENT.md"
      to: "app/Jobs/CloseMonthJob.php"
      via: "supervisor config runs queue:work for the close job"
      pattern: "queue:work"
    - from: "AGENTS.md Domain Walkthrough"
      to: "app/Services/BillPreviewService.php"
      via: "explains the bill math the service implements"
      pattern: "meal_rate"
---

<objective>
Docs + Deployment + Real-mess pilot. Wave 3 (after mobile/perf/coverage land in Plan 02 and seeder/tooling in Plan 01): (1) rewrite README in full with demo creds from Plan 01's seeder; (2) refresh AGENTS.md auto-gen map + add hand-written domain walkthrough; (3) write DEPLOYMENT.md production-hardening checklist; (4) clear the 4 Phase 4 HUMAN-UAT items; (5) prepare + run the one-mess pilot (D-01..D-05). The pilot itself is human/manual — the plan prepares it and the dev runs it.

Purpose: This is the v1 milestone ship. After this plan, the app has docs, a deploy target, cleared human-UAT, and one real mess running a clean monthly cycle. Phases 1-4 built the app; Plans 01-02 polished it; Plan 03 ships it.

Output: README + AGENTS.md + DEPLOYMENT.md + cleared HUMAN-UAT + pilot runbook + (human-run) pilot completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-polish-pilot/05-CONTEXT.md
@.planning/phases/05-polish-pilot/05-RESEARCH.md
@.planning/phases/04-reports-dashboard/04-HUMAN-UAT.md
@.planning/phases/05-polish-pilot/05-01-PLAN.md
@.planning/phases/05-polish-pilot/05-02-PLAN.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/INTEGRATIONS.md
@README.md
@AGENTS.md

<interfaces>
<!-- Existing artifacts Plan 03 consumes -->

Demo creds from Plan 01's PerfDemoSeeder (D-16 README uses these verbatim):
- Demo Manager: email `manager@demo.test`, password `password`, role `admin`
- Demo Member: email `member@demo.test`, password `password`, role `user`
- Demo Mess: name `Demo Mess` (the single mess with ~50 members + a full month of data)

Cache key strategy (D-17 AGENTS.md walkthrough must explain):
- `bill-preview:{mess_id}:{YYYY}-{MM}` — 1h TTL, invalidated on saved/deleted of MealEntry/MealOffRequest/GuestMeal/Expense/Payment
- `dash:counts:{mess_id}:{YYYY}-{MM}` — 1h TTL, same invalidation hook (Phase 4 D-17)

Bill math (D-17 walkthrough — verified against BillPreviewService):
- meal_rate = total_bazar / total_meals (bazar-only — fixed expenses do NOT enter the meal rate, FIXED-04)
- fixed_share = total_fixed / active_member_count (prorated by days for mid-month joiners/leavers, FIXED-03)
- member_bill = (member_meals × meal_rate) + fixed_share - bill_payments - advance_applied
- advance/due carry-forward: positive net_bill → due_balance; negative → advance_balance (next month)

Month-close flow (D-17 walkthrough — verified against CloseMonthJob + EnsureMonthIsOpen):
- Queued (CLOSE-02) via CloseMonthJob on the `database` queue
- Idempotent via UNIQUE(mess_id, year, month) + firstOrCreate + wasRecentlyCreated (CLOSE-07)
- Hard-locked post-close via EnsureMonthIsOpen middleware on 11 routes (CLOSE-10)
- Immutable snapshot to monthly_closings + monthly_member_summaries (CLOSE-09)
- Corrections via monthly_corrections table (snapshot stays immutable, CLOSE-12)

Role/IDOR model (D-17 walkthrough — verified against MyReportController):
- Roles: super-admin, admin, user (STATE.md)
- Member routes have NO `{member}` URL param — member derived from $request->user()->getMemberOrNull() (IDOR-structurally-impossible, T-04-02-01)
- Manager routes under role:admin + EnsureMessExists middleware
- Cross-mess member access → 404 via Member::firstOrFail() + MessScope

Tech stack (STACK.md, for README prerequisites):
- PHP 8.4, MySQL 8+, Node 24 (npm), Laravel 13.15
- Composer packages: Tyro, owen-it/auditing, maatwebsite/excel, barryvdh/laravel-dompdf, debugbar+telescope (require-dev)
- npm: chart.js, tailwind v4 via @tailwindcss/vite

Deployment target (D-15):
- VPS via Laravel Forge (primary) OR manual setup
- supervisor for queue:work (CloseMonthJob)
- schedule:run cron (daily telescope:prune + any scheduled commands)
- HTTPS + APP_URL + APP_DEBUG=false
- Shared hosting RULED OUT (cannot run persistent worker for the queued close)

Pilot logistics (D-01..D-05):
- D-01: real mess the dev has direct access to (own/family/close contact)
- D-02: fresh-start the current month — NO historical importer
- D-03: hybrid onboarding — dev configures mess+members+settings; manager runs daily ops
- D-04: success bar = ONE clean month-close + members view own bills + zero data-loss/math bugs
- D-05: feedback via direct WhatsApp/call with the manager

The 4 Phase 4 HUMAN-UAT items (D-23 — must clear):
1. Chart visual rendering (DASH-02) — 3 charts visible on /home with axes/labels/data
2. PDF export layout (RPT-07, D-13) — branded header, "Page N" footer, A4 portrait
3. Mobile responsive layout (D-04, DASH-04) — /my at 375px, 4 cards stack, no overflow
4. End-to-end cache refresh (DASH-05) — POST bazar, /home updates within ~2s
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite README + refresh AGENTS.md + write DEPLOYMENT.md (D-15, D-16, D-17, D-18)</name>
  <files>README.md, AGENTS.md, DEPLOYMENT.md</files>

  <read_first>
    - .planning/phases/05-polish-pilot/05-CONTEXT.md (D-16 README exact contents list; D-17 AGENTS.md walkthrough topics; D-18 DEPLOYMENT.md checklist + .env parity already done in Plan 01)
    - .planning/phases/05-polish-pilot/05-RESEARCH.md (§Code Examples Example 5 supervisor config verbatim — copy into DEPLOYMENT.md; §Architecture Patterns Pattern 1+2 — Telescope/Debugbar gating summary for DEPLOYMENT.md)
    - README.md (current state — likely the default Laravel stub per CONCERNS #17; rewrite fully)
    - AGENTS.md (current state — find the auto-gen section boundary to know where hand-written content goes)
    - .planning/codebase/STACK.md (versions for prerequisites section)
    - .planning/codebase/ARCHITECTURE.md (layer structure for the walkthrough)
    - .planning/codebase/INTEGRATIONS.md (queue = database driver, cache = database driver — both matter for DEPLOYMENT.md supervisor + cache priming)
    - app/Services/BillPreviewService.php (the bill math the AGENTS.md walkthrough explains — read the formulas verbatim)
    - app/Jobs/CloseMonthJob.php (the close flow the AGENTS.md walkthrough explains)
    - app/Http/Controllers/MyReportController.php (the IDOR model — no {member} param)
    - app/Providers/AppServiceProvider.php (the cache invalidation hook the walkthrough explains)
    - routes/web.php (the route list — EnsureMonthIsOpen middleware on 11 routes per STATE.md)
    - Plan 01's PerfDemoSeeder (the demo creds source — manager@demo.test / member@demo.test)
  </read_first>

  <action>
**A. README.md full rewrite (D-16):**
Replace the default Laravel stub entirely. Structure (required sections):
1. **Title + 1-paragraph pitch**: "Devsroom Mess Management — A mess manager can run a full month end-to-end on a phone." (use the PROJECT.md core value verbatim)
2. **What it does** (bullet list): member CRUD, daily meal grid, meal off + approvals, guest meals, bazar/fixed expenses, payments + advance balance, live bill preview, month-close (queued + idempotent + hard-locked), 4 reports (monthly/member statement/expense/payment), manager dashboard with 3 Chart.js charts, member /my dashboard, PDF + Excel exports
3. **Prerequisites**: PHP 8.4+, MySQL 8+, Node.js 24+ (npm), Composer. PHP extensions: pdo_mysql, gd, zip, mbstring, curl. (Link DEPLOYMENT.md for prod.)
4. **Installation (local dev)**: `git clone`, `composer install`, `npm install`, `npm run build` (or `npm run dev` for Vite HMR), `cp .env.example .env`, `php artisan key:generate`, edit `.env` MySQL block, `php artisan migrate`, `php artisan db:seed` (default seeder creates ExpenseCategorySeeder + test user)
5. **Demo dataset (optional)**: `php artisan db:seed:perf-demo` produces ~50 members + a full month of data. Demo creds: **Demo Manager** `manager@demo.test` / `password`; **Demo Member** `member@demo.test` / `password`. (Reference Plan 01's PerfDemoSeeder.)
6. **Common commands**: `vendor/bin/pint --test` (lint), `vendor/bin/phpunit` (tests), `php artisan test`, `php artisan serve`, `php artisan queue:work` (for month-close), `php artisan schedule:run`, `php artisan tinker`
7. **Perf/debug tooling (dev-only)**: Debugbar at the bottom of every page in local; Telescope at `/telescope`. Both require-dev, never ship to prod. (Cross-ref DEPLOYMENT.md.)
8. **Architecture overview** (1 paragraph + link to AGENTS.md for depth): Laravel 13 + Blade + Tailwind v4 + Tyro Dashboard + MySQL. Service layer (16 services), no Repository pattern. Single mess in v1, mess_id on all tables for v2 multi-tenant readiness.
9. **Deployment**: link DEPLOYMENT.md. Note shared hosting is ruled out (queue worker requirement).
10. **License / contributing / v2 roadmap**: link to .planning/REQUIREMENTS.md v2 section.

**B. AGENTS.md refresh + Domain Walkthrough (D-17):**
1. **Re-run the auto-gen updater** if one exists in the project (check `package.json` scripts or `composer.json` scripts for an "agents:update" or similar). If no updater exists, manually update the codebase map sections (file tree, service list, route list) to reflect Phases 2-4 additions (16 services, 4 report controllers, dashboard, exports, notifications, close + corrections). DO NOT delete existing accurate map content; refresh counts + paths.
2. **Append a "## Domain Walkthrough" section** (hand-written, this is the load-bearing content). Cover these 4 topics verbatim from the interfaces block above:
   - **Bill math**: meal_rate = total_bazar / total_meals (bazar-only, FIXED-04 excludes fixed). fixed_share = total_fixed / active_member_count (prorated by days for mid-month joiners, FIXED-03). member_bill = (meals × meal_rate) + fixed_share - bill_payments - advance_applied. Advance/due carry-forward: positive net_bill → due_balance; negative → advance_balance. Link to `app/Services/BillPreviewService.php` as the source of truth.
   - **Month-close flow**: Queued via `app/Jobs/CloseMonthJob.php` (CLOSE-02). Idempotent via UNIQUE(mess_id, year, month) + firstOrCreate + wasRecentlyCreated (CLOSE-07). Hard-locked post-close via `app/Http/Middleware/EnsureMonthIsOpen.php` applied to 11 routes (CLOSE-10). Immutable snapshot to `monthly_closings` + `monthly_member_summaries` (CLOSE-09). Corrections via `monthly_corrections` table — snapshot stays immutable (CLOSE-12). Math reuses BillPreviewService verbatim.
   - **Cache key strategy**: Two keys per (mess, year, month): `bill-preview:{mess_id}:{YYYY}-{MM}` (Phase 3 D-14, 1h TTL) and `dash:counts:{mess_id}:{YYYY}-{MM}` (Phase 4 D-17, 1h TTL). Both database driver, no tags. Invalidated on `saved`+`deleted` of MealEntry, MealOffRequest, GuestMeal, Expense, Payment via `app/Providers/AppServiceProvider.php::invalidateForModel()`. Keys are mess-scoped (T-04-03-01 prevents cross-mess bleed).
   - **Role/IDOR model**: Roles verbatim `super-admin`, `admin`, `user`. Member routes (`role:user`) have NO `{member}` URL parameter — `MyReportController` derives member from `$request->user()->getMemberOrNull()` (T-04-02-01 IDOR-structurally-impossible). Manager routes under `role:admin` + `EnsureMessExists`. Cross-mess member access returns 404 via `Member::firstOrFail()` + `MessScope` global scope.

**C. DEPLOYMENT.md production-hardening checklist (D-15, D-18):**
New file at repo root. Structure:
1. **Deployment target**: VPS (DigitalOcean/Hetzner/etc.) via Laravel Forge (primary, faster to ship) OR manual setup. Shared hosting RULED OUT — CloseMonthJob requires a persistent queue worker.
2. **Prerequisites (prod)**: PHP 8.4 + extensions (pdo_mysql, gd, zip, mbstring), MySQL 8+, Nginx, supervisor, Composer. Node only needed if building assets on the server (otherwise build locally + commit public/build).
3. **Forge path (primary)**: create site, set deployment script (`composer install --no-dev --optimize-autoloader`, `php artisan migrate --force`, `php artisan config:cache`, `php artisan route:cache`, `php artisan view:cache`, `npm ci && npm run build && php artisan filament:assets` if needed). Set environment variables in Forge UI. Add a Queue Worker (Forge writes the supervisor config). Add the Scheduler (Forge writes the cron).
4. **Manual VPS path (appendix)**: nginx site config, the supervisor config verbatim from research Example 5 (the `[program:mess-worker]` block with `command=php /var/www/mess/artisan queue:work database --sleep=3 --tries=3 --max-time=3600`, `numprocs=1`, `stopwaitsecs=3600`), and the schedule cron `* * * * * cd /var/www/mess && php artisan schedule:run >> /dev/null 2>&1`.
5. **Production .env checklist (HARD requirements)**:
   - `APP_ENV=production`
   - `APP_DEBUG=false` (CRITICAL — CONCERNS #9; if true, stack traces leak)
   - `APP_URL=https://your-domain.com` (HTTPS, not HTTP)
   - `APP_TIMEZONE=Asia/Dhaka` (carry-forward from Plan 01)
   - `DB_CONNECTION=mysql` + prod MySQL creds (NOT sqlite, NOT dev creds)
   - `QUEUE_CONNECTION=database` (supervisor runs the worker)
   - `CACHE_STORE=database` (or array/file for single-mess — database is fine)
   - `TELESCOPE_ENABLED=false` (Plan 01 default — verify)
   - `DEBUGBAR_ENABLED=false` (Plan 01 default — verify; also require-dev so `composer install --no-dev` won't even load it)
6. **HTTPS**: Let's Encrypt via Forge one-click OR certbot manually. Redirect HTTP → HTTPS in nginx. Set `ASSET_URL` if assets on a CDN.
7. **Storage permissions**: `storage/` and `bootstrap/cache/` writable by the web user (`chown -R www-data:www-data storage bootstrap/cache`). Profile photos + bazar receipts land in `storage/app/public/` — run `php artisan storage:link`.
8. **First deploy verification**: visit APP_URL, login, run a smoke (create a member, enter a meal, view the dashboard), trigger a test month-close, verify telescope:prune + queue:work are running (`sudo supervisorctl status`, `crontab -l`).
9. **Post-deploy monitoring**: Laravel logs at `storage/logs/laravel.log`. Worker log per supervisor config. Forge has a log UI.

**D. Verify docs reference reality:**
After writing, grep each doc for the demo creds, cache keys, role names — they MUST match the codebase exactly (manager@demo.test / bill-preview: / super-admin etc.). No fictional commands or paths.

  </action>

  <verify>
    <automated>test -f README.md &amp;&amp; test -f AGENTS.md &amp;&amp; test -f DEPLOYMENT.md &amp;&amp; grep -q "## Installation" README.md &amp;&amp; grep -q "manager@demo.test" README.md &amp;&amp; grep -q "Domain Walkthrough" AGENTS.md &amp;&amp; grep -q "APP_DEBUG=false" DEPLOYMENT.md &amp;&amp; grep -q "queue:work" DEPLOYMENT.md</automated>
  </verify>

  <acceptance_criteria>
    - `README.md` contains all 10 required sections (grep for "## Installation", "Demo Manager" or "manager@demo.test", "Prerequisites", "Common commands", "Deployment")
    - README.md does NOT contain the default Laravel stub text "Laravel has the most extensive and thorough documentation" or similar (grep returns nothing)
    - README.md demo creds match Plan 01's PerfDemoSeeder exactly: `manager@demo.test` and `member@demo.test` (grep both)
    - `AGENTS.md` contains a "## Domain Walkthrough" section
    - AGENTS.md Domain Walkthrough mentions all 4 required topics: bill math (grep "meal_rate"), month-close (grep "CloseMonthJob" or "EnsureMonthIsOpen"), cache key strategy (grep "bill-preview:"), role/IDOR model (grep "viewTelescope" or "super-admin" or "MessScope")
    - `DEPLOYMENT.md` contains: "APP_DEBUG=false", "supervisor" or "queue:work", "schedule:run", "HTTPS" or "APP_URL", "storage" permissions
    - DEPLOYMENT.md explicitly states shared hosting is ruled out (grep "shared hosting")
    - DEPLOYMENT.md contains the verbatim supervisor `[program:...]` config block (grep "program:mess-worker" or equivalent)
    - All 3 docs reference real codebase artifacts (no fictional file paths or commands) — spot-check via `rg "<referenced path>" app/ database/` for any path mentioned in the docs
  </acceptance_criteria>

  <done>
    README is a full rewrite with setup + demo creds + commands. AGENTS.md has refreshed map + hand-written Domain Walkthrough covering bill math, month-close, cache, IDOR. DEPLOYMENT.md is a production checklist with Forge path + manual VPS appendix + supervisor config + the hard APP_DEBUG=false requirement.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Clear the 4 Phase 4 HUMAN-UAT items + prepare pilot runbook (D-23, D-03)</name>
  <files>.planning/phases/04-reports-dashboard/04-HUMAN-UAT.md, .planning/phases/05-polish-pilot/05-PILOT-RUNBOOK.md</files>

  <read_first>
    - .planning/phases/04-reports-dashboard/04-HUMAN-UAT.md (the 4 items to clear — current state has all 4 as [pending])
    - .planning/phases/05-polish-pilot/05-CONTEXT.md (D-23 — these are the bridge from "automated tests green" to "human confirms it works"; D-01..D-05 pilot logistics)
    - .planning/phases/05-polish-pilot/05-RESEARCH.md (§Validation Architecture table — the 4 HUMAN-UAT items mapped to D-23)
    - .planning/phases/05-polish-pilot/05-02-PLAN.md (Plan 02's mobile audit covers HUMAN-UAT #3 partially; perf measurement covers #4 partially)
    - .planning/phases/05-polish-pilot/05-VERIFICATION.md (Plan 02's mobile + perf + coverage record — append HUMAN-UAT results here too)
    - Plan 01's PerfDemoSeeder output (the demo dataset to test against)
  </read_first>

  <action>

    <what-built>
Plans 01 and 02 delivered: a reproducible ~50-member demo dataset (D-07), Debugbar + Telescope dev tooling (D-06), a mobile responsive audit at 4 breakpoints with touch-target fixes (D-11/D-12), the 4 perf budgets measured (D-10), and test coverage >70% (D-22). This task asks the HUMAN to do the final visual/interactive verification that automated tests cannot: confirm charts actually render, PDF actually looks right, mobile actually stacks correctly, and cache actually refreshes after a write. Then it writes the pilot runbook for the dev to follow when onboarding the real mess.
  </what-built>

  

  <how-to-verify>
**Setup (do once):**
```bash
php artisan db:seed:perf-demo       # ~50 members + full month (Plan 01)
php artisan serve                   # or the dev composer script
```
Login as Demo Manager (`manager@demo.test` / `password`).

**HUMAN-UAT #1 — Chart rendering (DASH-02):**
1. Browse to `http://localhost:8000/home` as Demo Manager.
2. EXPECTED: 3 Chart.js charts render — Meal Trend (line, 30 days), Expense Trend (bar, bazar-only, 6 months), Payment Trend (bar, all methods, 6 months). Each chart has visible axes, labels, and data points. Canvas height ~280px. Changing a chart's date range via its form updates only that chart (hidden inputs preserve the other two).
3. Record in `04-HUMAN-UAT.md` item #1: result `pass` or describe the issue. Screenshot optional.

**HUMAN-UAT #2 — PDF export layout (RPT-07, D-13):**
1. Browse to `http://localhost:8000/mess/reports/monthly` as Demo Manager. Click the PDF export button (or visit `/mess/reports/monthly.pdf`).
2. EXPECTED: branded header with mess name + report title, plain CSS tables with borders, footer reads "Page N" (NOT "Page N of M"), content fits A4 portrait, per-member table uses compact font for the wide column set.
3. Record in `04-HUMAN-UAT.md` item #2: result `pass` or describe the issue.

**HUMAN-UAT #3 — Mobile responsive (D-04, DASH-04):**
1. Open Chrome/Edge DevTools → Device Toolbar → iPhone SE (375×667).
2. Login as Demo Member (`member@demo.test` / `password`). Browse to `http://localhost:8000/my`.
3. EXPECTED: 4 Overview cards stack vertically (single column) and remain legible. "My Payment History" panel shows amount + method pill + View-all link. NO horizontal overflow.
4. Record in `04-HUMAN-UAT.md` item #3: result `pass` or describe the issue.

**HUMAN-UAT #4 — End-to-end cache refresh (DASH-05):**
1. As Demo Manager, browse to `http://localhost:8000/home`. Note the "Monthly Expenses" card value and the Expense Trend chart.
2. Browse to `/mess/bazar` (or `/mess/expenses`) and POST a new bazar expense (e.g. amount 500).
3. Browse back to `/home`. Reload.
4. EXPECTED: within ~2 seconds the "Monthly Expenses" card and Expense Trend chart reflect the new expense.
5. Record in `04-HUMAN-UAT.md` item #4: result `pass` or describe the issue.

**Pilot runbook (05-PILOT-RUNBOOK.md) — Claude drafts, human reviews:**
While the human runs the 4 UAT items above, Claude drafts `05-PILOT-RUNBOOK.md` with:
- **Section 1: Hybrid Onboarding (D-03)** — step-by-step for the dev to configure the real mess (create the Mess row, create Members for each real person, configure settings: meal values, currency, date format). Notes the manager is NOT expected to self-serve; the dev screen-shares or does it in person.
- **Section 2: Daily Ops Runbook (for the manager)** — short guide: "each day, login as Manager, go to /mess/meals, check meals for today, log bazar expenses as they happen, take payments when received." Plain language, no jargon.
- **Section 3: Month-Close Runbook** — when the dev/manager is ready to close: trigger close from `/mess/close`, wait for the notification, verify members can view their bills.
- **Section 4: Success Criteria (D-04)** — one clean month-close + members view own bills + zero data-loss/math bugs. NOT two cycles, NOT formal sign-off.
- **Section 5: Feedback Channel (D-05)** — direct WhatsApp/call with the manager. No in-app feature.
- **Section 6: Fresh-start note (D-02)** — the pilot month IS the first recorded month. NO historical importer. If a second mess later needs one, that's post-pilot scope.

  </how-to-verify

  </action>

  

  <verify>

    HUMAN verification per the <how-to-verify> steps embedded in <action> above. Resume only when the human provides the resume-signal.

  </verify>

  

  <resume-signal>Type "approved" if all 4 UAT items pass and the pilot runbook is acceptable. If any UAT item fails, describe the issue — Claude will fix in code and re-run before pilot.</resume-signal>

  <acceptance_criteria>
    - `.planning/phases/04-reports-dashboard/04-HUMAN-UAT.md` has all 4 items with non-[pending] results (grep returns no "[pending]" — each item is `pass` or describes a specific issue that was then fixed)
    - `.planning/phases/05-polish-pilot/05-PILOT-RUNBOOK.md` exists and contains the strings "Hybrid Onboarding", "Daily Ops", "Month-Close", "Success Criteria", "WhatsApp" (D-05 feedback channel)
    - 05-PILOT-RUNBOOK.md references the demo creds from Plan 01 (manager@demo.test) as the example login (or notes the pilot uses real manager creds set during onboarding)
    - 05-PILOT-RUNBOOK.md explicitly states the pilot is fresh-start (no importer) per D-02
    - 05-PILOT-RUNBOOK.md success criteria matches D-04 verbatim: one clean month-close + members view own bills + zero data-loss/math bugs
    - If any of the 4 HUMAN-UAT items surfaced a code issue, the corresponding view/service file shows the fix in git diff (e.g., chart init bug, PDF footer rendering bug, mobile overflow, cache invalidation miss)
    - The human approved (typed "approved" or equivalent) before this task is marked complete
  </acceptance_criteria>

  <done>
    All 4 Phase 4 HUMAN-UAT items cleared (pass, or issue found + fixed + re-verified). Pilot runbook written with hybrid onboarding, daily ops, month-close, success criteria, and the WhatsApp feedback channel. Human has approved. The pilot is ready to run.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Run the one-mess pilot — deploy + onboard + one clean month-close (D-01..D-05, success #12)</name>
  <files>.planning/phases/05-polish-pilot/05-PILOT-RESULTS.md</files>

  <read_first>
    - .planning/phases/05-polish-pilot/05-PILOT-RUNBOOK.md (drafted in Task 2 — this is the playbook)
    - .planning/phases/05-polish-pilot/05-CONTEXT.md (D-01..D-05 — the pilot rules)
    - DEPLOYMENT.md (Task 1 — the deploy checklist)
    - .planning/phases/05-polish-pilot/05-VERIFICATION.md (Plan 02 + Task 2 — all automated + human checks done)
  </read_first>

  <action>

    <what-built>
This is NOT Claude's work. This is the human (the dev) running the actual pilot on a real mess per D-01..D-05. Claude's role is to: (a) write the results template (05-PILOT-RESULTS.md) the human fills in, (b) be on standby for any bug reports via the WhatsApp channel (D-05). The pilot cannot be automated — it requires a real mess, real members, real daily ops over a real month.
  </what-built>

  

  <how-to-verify>
**Human-run pilot (autonomous: false). The dev:**

1. **Provision the production deploy** per DEPLOYMENT.md (VPS via Forge OR manual). Verify APP_DEBUG=false, HTTPS, queue worker running, schedule cron running. Login as the production admin (real creds, NOT demo creds).

2. **Configure the real mess** per the Pilot Runbook §1 Hybrid Onboarding (D-03): create the Mess row with real name/address/rent, create a Member for each real person (real names, mobiles, joining dates), configure meal values + currency + date format in settings. The dev does this screen-share or in person; the manager is NOT expected to self-serve.

3. **Hand off daily ops to the manager** per Runbook §2. The manager: logs in daily, enters meals on `/mess/meals`, logs bazar on `/mess/bazar`, takes payments on `/mess/payments`. The dev is available via WhatsApp (D-05) for questions or bug reports.

4. **Run the month-close** per Runbook §3 when the month ends: trigger close from `/mess/close`, wait for the notification (NOTIF-01), verify members can view their own bills on `/my`.

5. **Record results in `05-PILOT-RESULTS.md`** (Claude drafts the template, the dev fills it in):
   - Pilot mess name + start date + close date
   - Member count
   - Month-close result (success / failure + error)
   - Members-view-bills result (verified / which members had issues)
   - Data-loss / math-bug count (must be ZERO per D-04)
   - Real-device mobile check (D-11 — the pilot phone is the final authority)
   - Any bugs found via WhatsApp (D-05) + their resolution
   - Overall verdict: PILOT PASS (D-04 success bar met) or PILOT FAIL (specific blocker)

**Success bar (D-04 — verbatim):**
- ONE clean month-close completes
- Members can view their own bills
- ZERO data-loss or math-wrong bugs

NOT required: two consecutive cycles, formal manager sign-off, "prefer over spreadsheet" gate.

**Claude's role during the pilot:**
- Draft the `05-PILOT-RESULTS.md` template (sections above) BEFORE the pilot starts
- Be on standby via the dev's channel for bug reports
- If a bug is reported, fix it in code, redeploy, document the fix in 05-PILOT-RESULTS.md
- Do NOT mark this task complete until the human confirms the pilot passed D-04

  </how-to-verify

  </action>

  

  <verify>

    HUMAN verification per the <how-to-verify> steps embedded in <action> above. Resume only when the human provides the resume-signal.

  </verify>

  

  <resume-signal>Type "pilot passed" when the month-close completes cleanly and members have verified their bills. Describe any bugs found + their resolution. If the pilot is blocked (e.g. the deploy failed, or a math bug surfaced), describe the blocker — Claude will assist.</resume-signal>

  <acceptance_criteria>
    - `.planning/phases/05-polish-pilot/05-PILOT-RESULTS.md` exists and is filled in by the dev (NOT [pending] / NOT a template)
    - 05-PILOT-RESULTS.md records: pilot mess name, start date, close date, member count, month-close result, members-view-bills result, data-loss/math-bug count
    - The recorded data-loss / math-bug count is ZERO (D-04 hard requirement)
    - 05-PILOT-RESULTS.md records a real-device mobile verification (D-11) — the pilot phone, not just DevTools
    - The production deploy is live (reachable HTTPS URL recorded in 05-PILOT-RESULTS.md)
    - The human confirmed "pilot passed" (or described a blocker that was then resolved)
    - All 12 ROADMAP Phase 5 success criteria are now met: #1 (Plan 02 Task 1), #2/#3/#4/#5/#6 (Plan 02 Task 2), #7 (Plan 01 Task 1), #8 (Plan 01 Task 1), #9 (Plan 02 Task 3), #10 (Plan 03 Task 1), #11 (Plan 03 Task 1), #12 (THIS task)
    - The v1 milestone is achieved: one real mess running a full monthly cycle, members viewing bills, no P0/P1 bugs
  </acceptance_criteria>

  <done>
    Pilot PASS per D-04: one clean month-close, members view own bills, zero data-loss/math bugs. Real-device mobile verified. Production deploy live. v1 milestone achieved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → production deploy | Production .env must NOT inherit dev secrets, demo creds, or APP_DEBUG=true |
| production .env → committed .env.example | .env.example must be sanitized (no real passwords); .env stays gitignored |
| pilot manager → production app | Manager logs in with real creds; IDOR model (no {member} URL) must hold under real use |
| pilot member → own bill | Member can view ONLY their own data — verified in Phase 4 but re-verified with real pilot members |
| public URL → app | HTTPS enforced; APP_DEBUG=false; Debugbar/Telescope do not ship (require-dev) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | APP_DEBUG=true shipped to production (CONCERNS #9) | mitigate | DEPLOYMENT.md hard requirement: `APP_DEBUG=false` in production .env. Task 1's DEPLOYMENT.md checklist item 5 lists it as a HARD requirement. Post-deploy verification step in DEPLOYMENT.md item 8 confirms it. |
| T-05-03-02 | Information Disclosure | Demo creds (manager@demo.test) accidentally used in production | mitigate | DEPLOYMENT.md notes demo creds are for local only. Production admin uses real creds set during deploy. PerfDemoSeeder is guarded (Plan 01 Task 3) — production `db:seed` does NOT run it. |
| T-05-03-03 | Spoofing | Pilot manager shares real creds, or weak password | accept | Tyro Login enforces lockout after 5 failed attempts (AUTH-04). Pilot manager is a trusted contact (D-01). Not a hardening target for v1. |
| T-05-03-04 | Tampering | Production deploy uses dev MySQL creds | mitigate | DEPLOYMENT.md checklist: prod MySQL creds set at deploy time, NOT copied from dev .env. The dev password (125524) is documented only in STATE.md internal notes, not in committed docs. |
| T-05-03-05 | Denial of Service | Queue worker dies on deploy, CloseMonthJob never runs | mitigate | DEPLOYMENT.md supervisor config with `autorestart=true` + Forge auto-restart-on-deploy. Post-deploy verification step: `sudo supervisorctl status` shows the worker RUNNING. |
| T-05-03-06 | Information Disclosure | Docs (README/AGENTS) leak internal architecture that aids attackers | accept | Architecture is already documented in `.planning/` (committed). Public README/AGENTS describe the system at a normal level of detail for an open-source-style project. No secrets in docs (demo creds are clearly labeled demo). |
| T-05-03-07 | Elevation of Privilege | Pilot reveals an IDOR hole (member sees another member's bill) | mitigate | T-04-02-01 verified in Phase 4: no `{member}` URL param on member routes. Pilot re-verifies with real members viewing their own bills. If a hole surfaces, fix + redeploy before pilot passes. |
| T-05-03-08 | Repudiation | Pilot math bug surfaces but isn't recorded | mitigate | 05-PILOT-RESULTS.md requires the data-loss/math-bug count to be explicitly ZERO (D-04). Any bug found via WhatsApp must be documented with its resolution. |
</threat_model>

<verification>
- Task 1: README + AGENTS.md + DEPLOYMENT.md all written with verifiable content (demo creds, domain walkthrough topics, supervisor config, APP_DEBUG=false requirement)
- Task 2: 4 Phase 4 HUMAN-UAT items cleared (human-verified) + pilot runbook written
- Task 3: pilot run on a real mess with results recorded (human-run, autonomous: false)
- Decision coverage: D-01 ✓ (Task 3), D-02 ✓ (Task 3 + runbook), D-03 ✓ (Task 2 runbook), D-04 ✓ (Task 3), D-05 ✓ (Task 2 runbook), D-15 ✓ (Task 1 + Task 3), D-16 ✓ (Task 1), D-17 ✓ (Task 1), D-18 ✓ (Task 1, .env parity already in Plan 01), D-23 ✓ (Task 2)
- PERF-13 (Pint) — no code in this plan; docs only
- All 12 ROADMAP Phase 5 success criteria met by end of plan
- v1 milestone achieved
</verification>

<success_criteria>
- README is a full rewrite with setup, demo creds, commands, deployment link
- AGENTS.md has refreshed map + hand-written Domain Walkthrough (bill math, month-close, cache, IDOR)
- DEPLOYMENT.md is a production checklist with Forge path + manual VPS + supervisor config + APP_DEBUG=false hard requirement
- 4 Phase 4 HUMAN-UAT items cleared
- Pilot runbook written
- Pilot PASS: one clean month-close + members view own bills + zero data-loss/math bugs (human-run)
- v1 milestone achieved — the app ships
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-pilot/05-03-docs-deployment-pilot-SUMMARY.md`

The v1 milestone SUMMARY should also be created at `.planning/MILESTONE-v1-SUMMARY.md` summarizing Phases 1-5 + the pilot outcome.
</output>
