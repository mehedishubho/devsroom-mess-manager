---
phase: 05-polish-pilot
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - resources/views/mess/meals/**/*.blade.php
  - resources/views/mess/bazar/**/*.blade.php
  - resources/views/mess/expenses/**/*.blade.php
  - resources/views/mess/payments/**/*.blade.php
  - resources/views/components/**/*.blade.php
  - tests/Feature/Perf/MealGridQueryCountTest.php
  - app/Services/MealGridService.php
  - app/Services/DashboardService.php
  - app/Services/BillPreviewService.php
  - app/Services/MemberStatementService.php
  - app/Services/ReportService.php
  - app/Jobs/CloseMonthJob.php
  - .planning/phases/05-polish-pilot/05-VERIFICATION.md
autonomous: true
requirements:
  - PERF-04
  - PERF-06
requirements_cross_plan:
  - PERF-13  # enforced in Plan 01 Wave 0; re-verified here on any touched file

must_haves:
  truths:
    - "All manager daily-ops screens (meal grid, bazar/expense entry, payments) render at 320/375/768/1024 without horizontal overflow at 360px+ (D-11, D-13)"
    - "Meal grid touch targets are ≥44px (min-h-[44px] min-w-[44px]) per the 01-UI-SPEC.md contract (D-12)"
    - "Daily grid loads in <100ms @50 members measured by Debugbar query time (NOT total request time) (D-10, success #2)"
    - "Dashboard loads in <500ms warm measured by Debugbar query time (D-10, success #3)"
    - "CloseMonthJob completes in <30s @50 members measured by Telescope Jobs tab (D-10, success #4)"
    - "Cache hit-rate >80% on repeat /home loads measured via Debugbar Cache tab pure-read loop (D-09, success #6)"
    - "Test coverage >70% measured via phpunit --coverage-text with targeted gap-fill tests added (D-22, success #9)"
    - "No N+1 query patterns remain in MealGridService, DashboardService, MemberStatementService, ReportService (D-10, success #5)"
  artifacts:
    - path: ".planning/phases/05-polish-pilot/05-VERIFICATION.md"
      provides: "Manual perf measurement record (4 budgets) + cache hit-rate + DevTools breakpoint checklist"
      contains: "<100ms"
    - path: "tests/Feature/Perf/MealGridQueryCountTest.php"
      provides: "Query-count smoke test locking the N+1 fix (D-08 says acceptable, not required)"
      contains: "enableQueryLog"
    - path: "resources/views/mess/meals/index.blade.php"
      provides: "Meal grid with 44px touch targets and 360px density pass"
      contains: "min-h-[44px]"
  key_links:
    - from: "Debugbar Queries tab"
      to: "app/Services/MealGridService.php"
      via: "verify whereIn pattern (2 queries, not N+1)"
      pattern: "whereIn.*member_id"
    - from: "Debugbar Cache tab"
      to: "app/Providers/AppServiceProvider.php"
      via: "verify bill-preview + dash:counts keys HIT on warm reload"
      pattern: "bill-preview:"
    - from: "Telescope Jobs tab"
      to: "app/Jobs/CloseMonthJob.php"
      via: "verify handle-time <30s @50"
      pattern: "CloseMonthJob"
---

<objective>
Mobile UX polish + Performance audit. Wave 2 (after Plan 01's seeder + Debugbar/Telescope land): (1) audit all manager daily-ops screens at 320/375/768/1024 via DevTools and run a meal-grid touch-target + density pass; (2) measure the four hard performance budgets using Debugbar query time/count (grid/dashboard) and Telescope Jobs tab (close) + Debugbar cache tab (hit-rate), fixing any N+1 or missing cache that surfaces; (3) measure test coverage >70% and add cheap meaningful gap-fill tests.

Purpose: This is where the perf budgets (success #2/#3/#4/#5/#6) and mobile readiness (success #1) actually get verified. Plan 01 built the seeder + tools; this plan uses them. Budgets are a HARD pass/fail gate (D-10) — a miss means fix the code, never relax the number.

Output: Polished mobile views, query-count smoke test, 05-VERIFICATION.md with manual measurement record, coverage report + gap-fill tests, any N+1/cache fixes landed in services.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-pilot/05-CONTEXT.md
@.planning/phases/05-polish-pilot/05-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/05-polish-pilot/05-01-PLAN.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.agents/skills/laravel-best-practices/SKILL.md
@app/Services/MealGridService.php
@app/Services/DashboardService.php
@app/Providers/AppServiceProvider.php

<interfaces>
<!-- Pre-verified facts the executor starts from (research §Critical verifications) -->

MealGridService::buildGridData is ALREADY N+1-safe (verified):
```php
// uses whereIn('member_id', $activeMembers->pluck('id')) for both MealEntry and MealOffRequest
// 2 queries, not N+1. Verify, don't rebuild. (research Example 1)
```

AppServiceProvider::invalidateForModel ALREADY forgets dash:counts (verified):
```php
// Same listener fires for saved+deleted on MealEntry, MealOffRequest, GuestMeal, Expense, Payment.
// Cache::forget("dash:counts:{$messId}:{$ym}") + Cache::forget("bill-preview:{$messId}:{$ym}")
// No rebuild needed. Verify, don't rebuild.
```

Budgets (D-10 — HARD gate):
- Grid: <100ms @50 members, measured as Debugbar QUERY TIME + COUNT (Pitfall 4 — total request time includes 50-150ms Debugbar overhead)
- Dashboard: <500ms warm, same measurement
- Close: <30s @50 members, measured in TELESCOPE Jobs tab handle-time (NOT Debugbar — jobs don't go through HTTP)
- Cache hit-rate: >80% measured via Debugbar Cache tab on REPEAT /home reloads (Pitfall 5 — pure-read loop, no writes between)

Cache keys that must HIT (verified):
- bill-preview:{mess_id}:{YYYY}-{MM}
- dash:counts:{mess_id}:{YYYY}-{MM}

01-UI-SPEC.md mobile contract (governs polish, no new design):
- Tailwind v4 + Blade only (no inline CSS, no shadcn)
- Touch target: min-h-[44px] min-w-[44px] (the `touch-target` utility exists in app.css)
- 375px baseline; D-13 sets 360px practical floor for Phase 5 (320px best-effort, NOT a hard gate)
- Color palette: emerald-600 accent (10%), slate-50 bg (60%), white card (30%)
- No JS/CSS in Blade, no HTML in PHP

Existing perf target services (read them, do NOT rewrite — fix only what fails):
- app/Services/MealGridService.php (the <100ms grid)
- app/Services/DashboardService.php (the <500ms dashboard)
- app/Services/BillPreviewService.php (cached bill math, no debug-throw — verified gone)
- app/Services/MemberStatementService.php + ReportService.php (reports)
- app/Jobs/CloseMonthJob.php (the <30s close)

Coverage tool: pcov (preferred) or xdebug — installed in Plan 01 Task 1 environment prep (if Plan 01 didn't install it, this plan's Task 3 cannot run; check first).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mobile UX audit at 320/375/768/1024 + meal-grid touch/density pass (D-11, D-12, D-13, D-14)</name>
  <files>resources/views/mess/meals/**/*.blade.php, resources/views/mess/bazar/**/*.blade.php, resources/views/mess/expenses/**/*.blade.php, resources/views/mess/payments/**/*.blade.php, resources/views/components/**/*.blade.php</files>

  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (THE governing design contract — §2.3 44px touch targets, §4.1 app layout, §4.2 form pages, §7 accessibility baseline; reuse Tailwind classes, no new design)
    - .planning/phases/05-polish-pilot/05-CONTEXT.md (D-11 DevTools at 4 breakpoints + real device in pilot; D-12 audit + meal-grid touch/density pass, NOT a rework; D-13 360px floor, 320px best-effort NOT a gate; D-14 manager daily-ops screens prioritized)
    - .planning/phases/05-polish-pilot/05-RESEARCH.md (§Mobile Polish — depth = audit + density, no bottom-sheet, no UX redesign)
    - resources/views/layouts/app.blade.php (the parent layout — drawer sidebar at <768px, w-64 at md+)
    - resources/views/mess/meals/index.blade.php (the densest screen — meal grid, the primary polish target)
    - resources/views/mess/bazar/*.blade.php, expenses/*.blade.php, payments/*.blade.php (manager daily-ops per D-14)
    - resources/views/components/ (existing button/input/form-field/card — touch-target utility may already exist; reuse)
    - resources/css/app.css (confirm the `touch-target` utility or `@layer components` block — 01-UI-SPEC §2.5)
    - Plan 01's PerfDemoSeeder output (login as Demo Manager, browse at 375px with real rows — this is what makes the density pass realistic)
  </read_first>

  <action>
**A. Set up the visual audit env:**
Run Plan 01's seeder: `php artisan db:seed:perf-demo`. Login as manager@demo.test (Demo Manager). Open Chrome/Edge DevTools → Device Toolbar. Set dimensions to 320×568, 375×667 (iPhone SE), 768×1024 (iPad), 1024×768 (desktop). At each, browse: `/home` (dashboard), `/mess/meals` (the grid), `/mess/bazar` (list + create), `/mess/expenses`, `/mess/payments`, `/my` (member side, lower priority per D-14).

**B. Document findings in `05-VERIFICATION.md` (a Mobile Responsive Audit section):**
For EACH of the priority screens (meal grid, bazar/expense entry, payments) at EACH breakpoint, record: PASS / horizontal-overflow / touch-target-too-small / text-truncation / unusable-density. Take screenshots if helpful.

**C. Meal grid touch-target pass (D-12):**
The meal grid is the densest, most-used manager screen. Every clickable cell (the B/L/D checkboxes, the per-member quick-action buttons "All on/All off/Breakfast only/Lunch only/Dinner only", the "Mark all 3 meals" / "Mark all 0 meals" presets) MUST have `min-h-[44px] min-w-[44px]` per 01-UI-SPEC §2.3. Audit each cell. If any cell is below 44px, add the `touch-target` utility class (from resources/css/app.css) OR explicit `min-h-[44px] min-w-[44px]` classes. Cells should remain tappable with a thumb on a 360px Android.

**D. Density pass for 360px (D-13):**
At 360px width the grid must not horizontal-scroll catastrophically. Options (in order of preference):
1. Compact cell padding (`p-1 sm:p-2`) — preserves all 3 meal columns + member name on one row
2. Smaller member name column (`text-xs sm:text-sm`, truncate long names with `truncate`)
3. Sticky first column (member name) + horizontal-scroll for the meal columns ONLY at <360px (last resort — D-13 says don't heavily compromise the grid for 320px)
At 320px: best-effort only. If the grid horizontal-scrolls at 320px, document as accepted per D-13.

**E. Form density (bazar/expense/payments):**
Forms should stack vertically on mobile (`flex flex-col gap-6` per 01-UI-SPEC §4.2). Sticky footer with Save button on mobile. Verify the existing form pages follow this. Fix any form that's 2-col on mobile (it should be `grid-cols-1 sm:grid-cols-2`).

**F. NO UX REWORK (D-12 explicit boundary):**
DO NOT introduce bottom-sheet patterns, modals, swipe gestures, or any new interaction model. This is a density + touch-target pass against the EXISTING 01-UI-SPEC.md contract. If a screen needs a redesign, document it as a follow-up — do not redesign in this task.

**G. Accessibility re-check (01-UI-SPEC §7):**
While auditing, verify: focus-visible:ring-2 ring-emerald-600 on all focusable elements; aria-label on icon-only buttons; semantic HTML (main/nav/header); form errors associated via aria-describedby. Fix any gaps found.

**H. Run Pint on touched files:**
`vendor/bin/pint --test resources/views/` — Blade files don't usually have Pint issues, but verify.

  </action>

  <verify>
    <automated>rg -n "min-h-\[44px\]|touch-target" resources/views/mess/meals/ &amp;&amp; test -f .planning/phases/05-polish-pilot/05-VERIFICATION.md &amp;&amp; vendor/bin/pint --test resources/views/</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/05-polish-pilot/05-VERIFICATION.md` exists and contains a "Mobile Responsive Audit" section with entries for each of the 4 breakpoints (320, 375, 768, 1024) for each manager daily-ops screen (meal grid, bazar, expenses, payments)
    - The meal grid view (`resources/views/mess/meals/index.blade.php` or equivalent) contains `min-h-[44px]` or `touch-target` on every clickable cell (grep returns matches in the meal grid file)
    - At 360px width in DevTools: the meal grid does NOT horizontal-scroll (documented PASS in 05-VERIFICATION.md), OR if it does, the sticky-first-column approach is implemented
    - At 320px width: best-effort only — horizontal scroll accepted per D-13 (document this in 05-VERIFICATION.md)
    - No new interaction patterns introduced (no `position: fixed` bottom sheets, no swipe libraries) — git diff shows only Tailwind class adjustments
    - `vendor/bin/pint --test resources/views/` exits 0
    - No view uses inline `style=""` (01-UI-SPEC constraint) — `rg "style=\"\"" resources/views/` returns nothing new
    - Demo Manager (manager@demo.test) can log in and navigate to all 4 priority screens without server errors (manual smoke before the audit)
  </acceptance_criteria>

  <done>
    Mobile audit documented at all 4 breakpoints. Meal grid touch targets ≥44px. 360px floor passes without catastrophic horizontal overflow. 320px best-effort documented. No UX rework. Accessibility re-check done.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Measure 4 perf budgets via Debugbar + Telescope, fix any N+1/cache miss (D-08, D-09, D-10)</name>
  <files>.planning/phases/05-polish-pilot/05-VERIFICATION.md, tests/Feature/Perf/MealGridQueryCountTest.php, app/Services/MealGridService.php, app/Services/DashboardService.php, app/Jobs/CloseMonthJob.php</files>

  <read_first>
    - .planning/phases/05-polish-pilot/05-RESEARCH.md (§Architecture Patterns Pattern 4 — manual measurement recipe verbatim; §Code Examples Example 1 N+1 detection, Example 2 fix options eager-load/select/cache, Example 3 cache hit-rate read, Example 6 Telescope Jobs read; §Common Pitfalls Pitfall 4 Debugbar adds 50-150ms overhead so use query time not total, Pitfall 5 cache hit-rate must be pure-read loop, Pitfall 6 already prevented by Plan 01's WithoutModelEvents)
    - .planning/phases/05-polish-pilot/05-CONTEXT.md (D-08 manual measurement into 05-VERIFICATION.md; D-09 cache hit-rate via Debugbar cache tab eyeballing; D-10 HARD gate — fix don't relax)
    - app/Services/MealGridService.php (verified N+1-safe — `whereIn('member_id', ...)` for both MealEntry + MealOffRequest; the grid budget MAY already pass, verify don't rebuild)
    - app/Services/DashboardService.php (uses bill-preview + dash:counts cache keys; verify the keys HIT on warm reload)
    - app/Providers/AppServiceProvider.php (the invalidateForModel hook — verified covers dash:counts; don't rebuild)
    - app/Jobs/CloseMonthJob.php (the <30s close — verify it queues + runs; Telescope captures handle-time)
    - app/Services/BillPreviewService.php (verified no debug-throw; the cache surface)
    - .agents/skills/laravel-best-practices/SKILL.md §1 Database Performance, §2 Advanced Queries, §4 Caching (the fix-options hierarchy)
  </read_first>

  <behavior>
    - Test 1: Loading /mess/meals with 50 active members runs <10 queries (DB::enableQueryLog → assert less than 10) — locks the existing whereIn N+1-safety so it can't regress (D-08 says acceptable, not required, but we add it because it's cheap)
    - Test 2: Loading /home as manager does NOT issue any repeated `select * from X where id = ?` pattern (N+1 signature)
  </behavior>

  <action>
**A. Create `tests/Feature/Perf/MealGridQueryCountTest.php` (D-08 optional, lock the N+1 fix):**
Follow research Example 4 verbatim. Seed 50 members via Member factory, enable query log, hit `/mess/meals` as manager, assert `<10` queries. Create the tests/Feature/Perf/ directory if missing (Plan 04 created .gitkeep files in Feature/ but maybe not Perf/). Run the test → it MUST pass (MealGridService is already N+1-safe per research verification). If it fails, that's a real N+1 — fix per the fix-options hierarchy below.

**B. Cold + warm measurement of /home + /mess/meals (Pattern 4 from research, verbatim):**
```bash
php artisan db:seed:perf-demo         # ~50 members + full month (Plan 01)
php artisan cache:clear               # cold-cache start
php artisan serve                     # or the dev composer script
```
Login as Demo Manager (manager@demo.test). Browse to `/home`. Click the Debugbar clock icon → Timeline. Record:
- QUERY COUNT (top-right of Queries tab)
- QUERY TIME (sum of query durations)
- TOTAL TIME (informational only — Pitfall 4 says this includes 50-150ms Debugbar overhead, NOT the budget metric)

Cold load (first) — should MISS both cache keys. NOT the budget number.
Warm load — reload 3 times. The 3rd warm reload's QUERY TIME is the dashboard budget number. Record in 05-VERIFICATION.md.

Repeat for `/mess/meals` — warm QUERY TIME is the grid budget number.

**C. CloseMonthJob timing via Telescope (Pattern 4 Step 5 + Example 6):**
```bash
php artisan queue:work --queue=default &  # background worker
```
Login as Demo Manager. Browse to `/mess/close`. Trigger close for current month. In Telescope (`/telescope/jobs`), find the CloseMonthJob entry. Record the HANDLE TIME (not wait time, not total). This is the <30s budget. Record in 05-VERIFICATION.md. Stop the worker after.

**D. Cache hit-rate via Debugbar Cache tab (Pattern 4 Step 6 + Example 3, Pitfall 5):**
Reload `/home` 5 times as Demo Manager. In Debugbar → Cache tab, count: `READ bill-preview:{mess_id}:{YYYY}-{MM} HIT` and `READ dash:counts:{mess_id}:{YYYY}-{MM} HIT`. The initial populate writes (MISS → MISS) don't count; warm steady-state is the >80% target. Pure-read loop — DO NOT submit any forms between reloads (would trigger invalidateForModel → Cache::forget → miss). Record hit count + total reads + computed rate.

**E. If a budget MISSES (D-10 — fix, never relax):**
Grid >100ms @50: Look at Debugbar Queries tab. If you see repeated `select * from meal_entries where member_id = ?` (N+1), apply fix per Example 2:
  - First: `with('relation')` eager load
  - Or: `addSelect(['subquery'])` for a single value
  - Or: cache the aggregate (dashboard already does this; grid is per-day so unlikely cached)
MealGridService is verified N+1-safe, so this is unlikely — but verify.
Dashboard >500ms: Check for new relations loaded without `with()`. Check that bill-preview + dash:counts keys HIT (if they MISS on warm reload, the cache strategy is broken — investigate AppServiceProvider hook).
Close >30s @50: Check CloseMonthJob for a per-member loop with a query inside. Optimize via batch insert or eager-loaded collections.
Cache <80%: Verify no incidental writes between reloads. If still low, check that invalidateForModel isn't firing spuriously (e.g., a saved event on an unrelated model).

**F. Record ALL measurements in 05-VERIFICATION.md:**
A "Performance Budgets" section with 4 subsections (Grid, Dashboard, Close, Cache Hit-Rate). Each has: the budget, the measured value, the measurement method (Debugbar query time vs Telescope handle time vs Debugbar cache tab), the PASS/FAIL verdict, and the fix applied if any. This document is the D-08 acceptance artifact.

**G. Cross-check sanity (Pitfall 4):**
For the wall-clock budgets, briefly disable Debugbar (`DEBUGBAR_ENABLED=false` in .env, `php artisan config:clear`) and re-measure via browser DevTools Network tab "Time" column. If the Debugbar reading is 5× the DevTools reading, the overhead is dominating — investigate. Re-enable Debugbar after.

  </action>

  <verify>
    <automated>vendor/bin/phpunit --filter=MealGridQueryCountTest &amp;&amp; test -f .planning/phases/05-polish-pilot/05-VERIFICATION.md &amp;&amp; grep -q "&lt;100ms" .planning/phases/05-polish-pilot/05-VERIFICATION.md &amp;&amp; grep -q "&lt;30s" .planning/phases/05-polish-pilot/05-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - `tests/Feature/Perf/MealGridQueryCountTest.php` exists and passes — asserts `/mess/meals` at 50 members runs <10 queries (locks the N+1 fix per D-08)
    - `.planning/phases/05-polish-pilot/05-VERIFICATION.md` contains a "Performance Budgets" section with all 4 budgets recorded (grep for "<100ms", "<500ms", "<30s", ">80%")
    - 05-VERIFICATION.md records each budget's MEASURED value, not just the target — the value is a real number from Debugbar/Telescope, not "passed"
    - 05-VERIFICATION.md records the MEASUREMENT METHOD for each (Debugbar query time for grid/dashboard, Telescope handle-time for close, Debugbar cache tab for hit-rate)
    - If any budget was missed, the corresponding service file (MealGridService/DashboardService/CloseMonthJob) shows the fix in git diff (NOT a relaxed number in 05-VERIFICATION.md)
    - Cache hit-rate recorded as a real percentage computed from the cache-tab read count
    - `vendor/bin/phpunit` (full suite) still passes — including the new MealGridQueryCountTest
  </acceptance_criteria>

  <done>
    All 4 perf budgets measured manually and recorded in 05-VERIFICATION.md with real numbers + measurement methods. Grid <100ms @50, dashboard <500ms, close <30s @50, cache >80% — or the underlying code fixed (not the budget relaxed). Query-count smoke test locks the grid N+1-safety. No regression in the full test suite.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Coverage measurement >70% + targeted gap-fill tests (D-22, success #9)</name>
  <files>tests/Feature/**/*.php, tests/Unit/**/*.php</files>

  <read_first>
    - .planning/phases/05-polish-pilot/05-CONTEXT.md (D-22 — measurement + targeted-fill, NOT blanket "write tests everywhere"; >70% target)
    - .planning/phases/05-polish-pilot/05-RESEARCH.md (§Environment Availability — pcov/xdebug BLOCKER flagged for Plan 01 Wave 0; §Common Pitfalls Pitfall 3 — use pcov not xdebug, faster)
    - .planning/codebase/TESTING.md (PHPUnit 12, RefreshDatabase, test_ prefix, existing 233 tests)
    - phpunit.xml (current config — confirm it has the <coverage> block or configure one)
    - Plan 01 Task 1 verification — confirm pcov (or xdebug) is now installed (if NOT, this task cannot run — return blocker note in 05-VERIFICATION.md)
    - The 05-VERIFICATION.md created in Task 2 (append the Coverage section to it)
  </read_first>

  <action>
**A. Verify the coverage driver is installed (BLOCKER check):**
```bash
php -m | grep -iE "pcov|xdebug"
```
If neither is loaded, this task is BLOCKED. Record in 05-VERIFICATION.md: "Coverage measurement blocked — pcov/xdebug not installed (Plan 01 Wave 0 task did not complete). Documented as known issue for post-Plan-02 follow-up." Then EXIT THIS TASK (do not attempt the rest).
If pcov or xdebug is loaded, continue.

**B. Run baseline coverage:**
```bash
vendor/bin/phpunit --coverage-text
```
Record the total % and the per-directory % in 05-VERIFICATION.md under a "Test Coverage" section. PHPUnit will list uncovered files/classes/methods.

**C. Identify cheap meaningful gap-fill targets:**
Look at the coverage report for:
- Services with <60% — usually business logic that has a clear input→output contract (BillPreviewService math, MemberStatementService, ChartBucketingService auto-bucket boundaries)
- Form Requests with <70% — validation rules are cheap to test
- Jobs with <70% — CloseMonthJob idempotency edge cases
DO NOT target:
- Boot/glue code with no behavior (providers' register methods, route files)
- Migration files
- Pure DTOs / enums without logic

**D. Add 5-15 targeted gap-fill tests (NOT blanket):**
Pick the 3-5 highest-value gaps. For each, write ONE test file or add ONE test method to an existing file. Examples (choose based on actual coverage gaps found):
- `tests/Unit/BillPreviewMathTest.php` — meal_rate edge cases (zero bazar, zero meals, mixed)
- `tests/Feature/Report/MemberStatementEdgeCasesTest.php` — first-of-month, mid-month joiner, no payments
- `tests/Feature/CloseMonth/CloseMonthProrationTest.php` — former-member proration in the snapshot

Re-run coverage after each batch:
```bash
vendor/bin/phpunit --coverage-text
```
Stop when total coverage >70% OR when the remaining gaps are all boot/glue code (whichever comes first — D-22 says targeted, not blanket).

**E. Pin Pint clean on new test files:**
```bash
vendor/bin/pint --test tests/
```

**F. Record final coverage in 05-VERIFICATION.md:**
"Coverage final: X% (target >70%). Gap-fill tests added: [list of files]. Remaining gaps (intentionally not covered): [boot/glue code list]."

  </action>

  <verify>
    <automated>php -m | grep -iE "pcov|xdebug" &amp;&amp; vendor/bin/phpunit --coverage-text 2>&amp;1 | tail -20 &amp;&amp; vendor/bin/pint --test tests/</automated>
  </verify>

  <acceptance_criteria>
    - EITHER: `php -m` shows pcov or xdebug loaded, AND `vendor/bin/phpunit --coverage-text` reports total coverage ≥70% (or documents why the remaining gaps are boot/glue), AND 05-VERIFICATION.md has a "Test Coverage" section recording the final %
    - OR: 05-VERIFICATION.md documents that pcov/xdebug is NOT installed and this task is blocked (with a follow-up note) — in which case the rest of the criteria are N/A
    - Any new test files added pass (run `vendor/bin/phpunit --filter=<new test>` to verify each)
    - `vendor/bin/pint --test tests/` exits 0
    - The full suite `vendor/bin/phpunit` passes (new tests don't regress)
    - 05-VERIFICATION.md records the gap-fill test files added (grep for "Gap-fill tests added")
    - If coverage is <70% after targeted fill, 05-VERIFICATION.md documents which specific gaps remain and why they were not filled (boot/glue code, no-behavior files)
  </acceptance_criteria>

  <done>
    Coverage measured via phpunit --coverage-text. >70% target met OR documented why it's not (pcov/xdebug unavailable, or remaining gaps are boot/glue). Targeted gap-fill tests added where cheap and meaningful — NOT a blanket test-writing pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DevTools → server response | Visual audit happens in the dev's browser; no new inputs cross a trust boundary |
| perf measurement → service code | Any N+1 fix changes a query path; must not introduce SQL injection or auth bypass |
| test fixtures → production DB | PerfDemoSeeder (Plan 01) already guarded; gap-fill tests use RefreshDatabase (sandbox DB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | N+1 fix introduces SQL injection via raw query | mitigate | Use only Eloquent `with()` / `addSelect()` / `whereIn()` per laravel-best-practices skill §1/§2. NO `DB::raw()` with user input. Existing parameterized queries already safe. |
| T-05-02-02 | Information Disclosure | Mobile density fix exposes another member's data via collapsed UI | mitigate | Audit each density change against the IDOR model (no `{member}` URL param on member routes — T-04-02-01). Verify no truncation accidentally hides the wrong member name in a shared view. |
| T-05-02-03 | Denial of Service | A missed perf budget left unfixed (relaxed instead) → prod grid takes 500ms @50 | mitigate | D-10 is a HARD gate. Acceptance criteria forbids relaxing the budget number — any miss MUST be a code fix in services, recorded in git diff. |
| T-05-02-04 | Repudiation | Coverage gap-fill tests bypass auth to make tests pass | mitigate | Follow existing testing conventions (TESTING.md): actingAs($manager) or actingAs($user) for role tests. No `withoutMiddleware()` on auth-sensitive paths. |
| T-05-02-05 | Information Disclosure | Cache hit-rate measurement accidentally reveals cache keys to a non-manager | accept | Measurement runs in local dev as Demo Manager. Cache keys are mess-scoped, not user-scoped. No real data exposure in dev. |
| T-05-02-06 | Tampering | MealGridQueryCountTest asserts a relaxed threshold (>100 queries instead of <10) | mitigate | Acceptance criteria pins the test at <10 queries (locks the verified whereIn pattern). If MealGridService regresses, the test fails loudly. |
</threat_model>

<verification>
- All 3 tasks' `<verify>` automated commands pass (or Task 3 is documented as blocked)
- Decision coverage: D-08 ✓ (Task 2), D-09 ✓ (Task 2), D-10 ✓ (Task 2), D-11 ✓ (Task 1), D-12 ✓ (Task 1), D-13 ✓ (Task 1), D-14 ✓ (Task 1), D-22 ✓ (Task 3)
- PERF-04 (mobile-first) ✓ (Task 1)
- PERF-06 (cache >80%) ✓ (Task 2)
- PERF-13 (Pint clean) ✓ (Task 1 + Task 3 verify blocks)
- 05-VERIFICATION.md exists with Mobile Responsive Audit + Performance Budgets + Test Coverage sections
- All 12 ROADMAP success criteria addressed by end of plan: #1 (Task 1), #2/#3/#4/#5/#6 (Task 2), #9 (Task 3) — #7/#8 landed in Plan 01, #10/#11 land in Plan 03, #12 is the pilot in Plan 03
</verification>

<success_criteria>
- All manager daily-ops screens render at 320/375/768/1024 with 360px floor passing
- Meal grid touch targets ≥44px, density pass done, no UX rework
- 4 perf budgets measured and recorded in 05-VERIFICATION.md with real numbers; any miss fixed in code (not relaxed)
- Cache hit-rate >80% on warm pure-read reloads
- Query-count smoke test locks the grid N+1-safety
- Coverage >70% (or documented blocker / boot-glue gaps)
- No test regression; full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-pilot/05-02-mobile-perf-coverage-SUMMARY.md`
</output>
