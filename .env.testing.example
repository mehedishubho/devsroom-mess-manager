# Copy to `.env.testing` (gitignored) and set your local MySQL test password.
# Laravel auto-loads `.env.testing` when APP_ENV=testing (set by phpunit.xml).
# phpunit.xml intentionally omits DB_PASSWORD so each developer supplies it
# locally rather than committing it to version control (see CR-02 of the
# phase-03 code review).
DB_PASSWORD=
